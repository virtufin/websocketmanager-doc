{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Virtufin WebSocketManager","text":"<p>Centralized WebSocket connection management service with gRPC API support and Dapr integration for distributed state and pub/sub messaging.</p>"},{"location":"#key-features","title":"Key Features","text":"Feature Description Connection Management Create, monitor, and control WebSocket connections via gRPC Distributed State Connection state persisted across instances via Dapr state store Pub/Sub Integration Publish WebSocket messages to Dapr pub/sub topics Auto-Reconnect Configurable automatic reconnection with health monitoring Multi-Language SDKs First-class clients for C# (.NET), Python, and TypeScript"},{"location":"#get-started","title":"Get Started","text":"<ul> <li>Getting Started \u2014 Install and run your first WebSocket connection</li> <li>Architecture \u2014 System components and data flow</li> <li>API Reference \u2014 Complete gRPC and REST API docs</li> <li>SDK Guides \u2014 Client library usage for C#, Python, and TypeScript</li> <li>Deployment \u2014 Docker, Kubernetes, and CI/CD</li> <li>Troubleshooting \u2014 Common issues and solutions</li> <li>Project README \u2014 Full project overview with Quick Start and build instructions</li> </ul>"},{"location":"api/","title":"API Reference","text":"<p>The WebSocket Manager exposes a gRPC-based API defined in <code>websocketmanager.proto</code>. All endpoints are available over HTTP/2 with optional JSON transcoding support.</p>"},{"location":"api/#generated-api-documentation","title":"Generated API Documentation","text":"<ul> <li>C# SDK Reference \u2014 Auto-generated docs for <code>Virtufin.WebSocketManager</code> and <code>Virtufin.WebSocketManager.Client</code></li> <li>Python SDK Reference \u2014 Auto-generated docs for <code>virtufin-websocketmanager</code> package</li> <li>TypeScript SDK Reference \u2014 Auto-generated docs for TypeScript client</li> </ul>"},{"location":"api/#table-of-contents","title":"Table of Contents","text":"<ul> <li>gRPC Endpoints</li> <li>Messages</li> <li>Enums</li> <li>Usage Examples</li> <li>HTTP/JSON Transcoded Endpoints</li> <li>Error Handling</li> </ul>"},{"location":"api/#grpc-endpoints","title":"gRPC Endpoints","text":"Method Type Description <code>Connect</code> Unary Establish a new WebSocket connection <code>List</code> Unary List all managed WebSocket connections <code>Disconnect</code> Unary Disconnect and remove a connection <code>StartPublish</code> Unary Start publishing messages to a Dapr topic <code>StopPublish</code> Unary Stop publishing messages <code>Send</code> Unary Send a message and wait for a correlated response <code>SendRaw</code> Unary Send a message without waiting for a response"},{"location":"api/#messages","title":"Messages","text":""},{"location":"api/#connectrequest","title":"ConnectRequest","text":"<p>Request to establish a new WebSocket connection.</p> Field Type Description <code>url</code> <code>string</code> WebSocket server URL to connect to (e.g., <code>ws://localhost:8080</code>) <code>auto_reconnect</code> <code>bool</code> Automatically reconnect on connection failure <code>tags</code> <code>map&lt;string, string&gt;</code> Optional resource tags for filtering/discovery <p>Response: <code>ConnectResponse</code></p>"},{"location":"api/#connectresponse","title":"ConnectResponse","text":"<p>Response returned after a successful connection.</p> Field Type Description <code>connection_id</code> <code>string</code> Unique identifier for the established connection <code>status</code> <code>ConnectionStatus</code> Current status of the connection"},{"location":"api/#listrequest","title":"ListRequest","text":"Field Type Description <code>filters</code> <code>repeated TagFilter</code> Optional filters (AND semantics). Only connections matching ALL filter key-value pairs are returned. Empty = no filter. <p>Response: <code>ListResponse</code></p>"},{"location":"api/#listresponse","title":"ListResponse","text":"Field Type Description <code>connections</code> <code>repeated WebSocketConnection</code> List of all WebSocket connections"},{"location":"api/#websocketconnection","title":"WebSocketConnection","text":"<p>Represents a managed external WebSocket connection.</p> Field Type Description <code>id</code> <code>string</code> Unique connection identifier <code>url</code> <code>string</code> WebSocket server URL <code>status</code> <code>ConnectionStatus</code> Current connection state <code>instance_id</code> <code>string</code> Owning service instance ID <code>topic</code> <code>string</code> Dapr pub/sub topic (when publishing) <code>tags</code> <code>map&lt;string, string&gt;</code> Currently-set resource tags (filtering/discovery \u2014 not forwarded to remote websocket) <code>created_at</code> <code>google.protobuf.Timestamp</code> Connection creation timestamp"},{"location":"api/#disconnectrequest","title":"DisconnectRequest","text":"Field Type Description <code>id</code> <code>string</code> ID of the connection to disconnect <p>Response: <code>DisconnectResponse</code> (empty)</p>"},{"location":"api/#startpublishrequest","title":"StartPublishRequest","text":"Field Type Description <code>id</code> <code>string</code> Connection ID to start publishing for <code>topic</code> <code>string</code> Dapr pub/sub topic name <p>Response: <code>StartPublishResponse</code> (empty)</p>"},{"location":"api/#stoppublishrequest","title":"StopPublishRequest","text":"Field Type Description <code>id</code> <code>string</code> Connection ID to stop publishing for <p>Response: <code>StopPublishResponse</code> (empty)</p>"},{"location":"api/#sendrequest","title":"SendRequest","text":"<p>Sends a message through a WebSocket connection and waits for a correlated response.</p> Field Type Description <code>id</code> <code>string</code> Connection ID to send through <code>message</code> <code>string</code> Message payload <code>timeout_ms</code> <code>int32</code> Timeout in milliseconds for the response <p>Response: <code>SendResponse</code></p>"},{"location":"api/#sendresponse","title":"SendResponse","text":"Field Type Description <code>response</code> <code>string</code> Response from the WebSocket server"},{"location":"api/#sendrawrequest","title":"SendRawRequest","text":"<p>Sends a message without waiting for a correlating response.</p> Field Type Description <code>id</code> <code>string</code> Connection ID to send through <code>message</code> <code>string</code> Message payload <p>Response: <code>SendRawResponse</code> (empty)</p>"},{"location":"api/#tag-rpcs","title":"Tag RPCs","text":"RPC HTTP Description <code>SetTag</code> <code>POST /v1/connections/{id}/tags/{key}</code> Set a single tag on a connection <code>GetTag</code> <code>GET /v1/connections/{id}/tags/{key}</code> Get a single tag value <code>SetTags</code> <code>PUT /v1/connections/{id}/tags</code> Replace all tags on a connection <code>GetTags</code> <code>GET /v1/connections/{id}/tags</code> Get all tags for a connection <p>Tags are resource metadata used for filtering and discovery. They are NOT forwarded to the remote websocket server. Limits: 50 tags per connection, key \u2264 128 chars, value \u2264 1024 chars.</p> <p><code>ListConnections</code> accepts <code>filters: repeated TagFilter { key, value }</code> \u2014 only connections matching ALL filter entries are returned (AND semantics). Empty filters = no filtering.</p>"},{"location":"api/#enums","title":"Enums","text":""},{"location":"api/#connectionstatus","title":"ConnectionStatus","text":"Value Description <code>UNKNOWN</code> Unknown status <code>CONNECTING</code> Connection attempt in progress <code>CONNECTED</code> Connected and ready <code>DISCONNECTED</code> Not connected <code>ERROR</code> Connection error state"},{"location":"api/#usage-examples","title":"Usage Examples","text":""},{"location":"api/#grpc-c","title":"gRPC (C#)","text":"<pre><code>using Virtufin.WebSocketManager.Client;\nusing Grpc.Net.Client;\n\nvar channel = GrpcChannel.ForAddress(\"https://localhost:5002\");\nvar client = new WebSocketManagerClient(channel);\n\nvar response = await client.ConnectAsync(new ConnectRequest\n{\n    Url = \"ws://echo.websocket.org\",\n    AutoReconnect = true\n});\n\nConsole.WriteLine($\"Connected: {response.ConnectionId}\");\n\nawait client.SendAsync(new SendRequest\n{\n    Id = response.ConnectionId,\n    Message = \"Hello WebSocket!\",\n    TimeoutMs = 5000\n});\n</code></pre>"},{"location":"api/#python","title":"Python","text":"<pre><code>from virtufin.websocketmanager.client import WebSocketManagerClient\n\nasync with WebSocketManagerClient(host=\"localhost\", port=5002) as client:\n    connection = await client.connect(\"ws://echo.websocket.org\")\n    print(f\"Connected: {connection.connection_id}\")\n\n    response = await client.send(connection.connection_id, \"Hello WebSocket!\", timeout_ms=5000)\n    print(f\"Response: {response.response}\")\n</code></pre>"},{"location":"api/#typescript","title":"TypeScript","text":"<pre><code>import { createClient } from \"@connectrpc/connect\";\nimport { WebSocketManager } from \"./generated/websocketmanager_pb.js\";\n\nconst client = createClient(WebSocketManager, transport);\n\nconst response = await client.connect({\n  url: \"ws://echo.websocket.org\",\n  autoReconnect: true\n});\n\nconsole.log(`Connected: ${response.connectionId}`);\n\nconst sendResponse = await client.send({\n  id: response.connectionId,\n  message: \"Hello WebSocket!\",\n  timeoutMs: 5000\n});\n</code></pre>"},{"location":"api/#httpjson-transcoded-endpoints","title":"HTTP/JSON Transcoded Endpoints","text":"<p>The following REST endpoints are available when JSON transcoding is enabled:</p> Method Path gRPC Equivalent <code>POST</code> <code>/v1/connect</code> <code>Connect</code> <code>GET</code> <code>/v1/connections</code> <code>List</code> <code>DELETE</code> <code>/v1/connections/{id}</code> <code>Disconnect</code> <code>POST</code> <code>/v1/connections/{id}/publish</code> <code>StartPublish</code> <code>DELETE</code> <code>/v1/connections/{id}/publish</code> <code>StopPublish</code> <code>POST</code> <code>/v1/connections/{id}/send</code> <code>Send</code> <code>POST</code> <code>/v1/connections/{id}/sendraw</code> <code>SendRaw</code>"},{"location":"api/#error-handling","title":"Error Handling","text":"<p>All gRPC methods return standard <code>grpc::StatusCode</code> values:</p> Status Scenario <code>NOT_FOUND</code> Connection ID not found <code>INVALID_ARGUMENT</code> Invalid URL or parameters <code>DEADLINE_EXCEEDED</code> Send timed out waiting for response <code>UNAVAILABLE</code> Server not reachable <code>ALREADY_EXISTS</code> Connection already established <code>INTERNAL</code> WebSocket protocol error"},{"location":"architecture/","title":"Architecture Overview","text":"<p>The Virtufin WebSocket Manager is a distributed WebSocket connection management service built on .NET with gRPC API support and Dapr integration for state persistence and pub/sub messaging.</p>"},{"location":"architecture/#system-architecture","title":"System Architecture","text":"<pre><code>flowchart TD\n    subgraph WSM[\"WebSocket Manager\"]\n        API[gRPC API&lt;br/&gt;Protobuf] --&gt; WS[WebSocketService]\n        WS --&gt; WSC[WebSocketClientWrapper]\n        WS --&gt; DS[DistributedStore]\n        DS --&gt; DR[DaprConnection&lt;br/&gt;Repository]\n        WSC --&gt; EXT[External&lt;br/&gt;WebSocket Servers]\n    end\n    DR --&gt; DAPR[Dapr Runtime]\n    DAPR --&gt; SS[(Dapr State Store)]\n    DAPR --&gt; PS[(Dapr Pub/Sub)]</code></pre>"},{"location":"architecture/#component-overview","title":"Component Overview","text":""},{"location":"architecture/#grpc-api-layer","title":"gRPC API Layer","text":"<p>The entry point for all client operations. The <code>WebSocketManagerService</code> implements the protobuf-defined gRPC service, handling: - Connection lifecycle (connect, disconnect, list) - Message publishing control (start/stop publish) - Message sending (send, send raw)</p>"},{"location":"architecture/#service-layer","title":"Service Layer","text":"<p>Core business logic implemented by <code>WebSocketService</code>. Orchestrates: - Connection creation and management - Message routing to Dapr pub/sub - Ownership validation against instance IDs</p>"},{"location":"architecture/#storage-layer","title":"Storage Layer","text":"<p>Distributed persistence via Dapr: - <code>DistributedWebSocketConnectionStore</code> - manages local cache + Dapr state - <code>DaprConnectionRepository</code> - handles raw Dapr state operations</p>"},{"location":"architecture/#client-layer","title":"Client Layer","text":"<p>Low-level WebSocket operations via <code>WebSocketClientWrapper</code>: - Connection establishment - Message sending/receiving - Auto-reconnect logic</p>"},{"location":"architecture/#integration-layer","title":"Integration Layer","text":"<ul> <li><code>DaprPublisher</code> - publishes messages to Dapr pub/sub topics</li> <li><code>InstanceIdProvider</code> - identifies which service instance owns each connection</li> </ul>"},{"location":"architecture/#data-flow","title":"Data Flow","text":""},{"location":"architecture/#connecting-to-a-websocket","title":"Connecting to a WebSocket","text":"<ol> <li>Client sends <code>Connect</code> gRPC request with URL and autoReconnect flag</li> <li><code>WebSocketManagerService</code> delegates to <code>WebSocketService</code></li> <li><code>WebSocketService</code> creates a <code>WebSocketConnection</code> via the store</li> <li><code>WebSocketClientWrapper</code> establishes the actual WebSocket connection</li> <li>Receive loop is started to handle incoming messages</li> <li>Connection is persisted to Dapr state store</li> <li>Connection ID is returned to client</li> </ol>"},{"location":"architecture/#publishing-messages","title":"Publishing Messages","text":"<ol> <li>External WebSocket server sends a message</li> <li><code>WebSocketClientWrapper</code> receive loop captures the message</li> <li>If connection has a topic configured, <code>DaprPublisher</code> is called</li> <li><code>DaprPublisher</code> publishes message to the configured Dapr pub/sub topic</li> <li>Subscribers receive the message with connection metadata (ID, URL, timestamp)</li> </ol>"},{"location":"architecture/#distributed-state","title":"Distributed State","text":"<ul> <li>Each service instance maintains a local cache (<code>ConcurrentDictionary</code>)</li> <li>All state is persisted to Dapr state store for cross-instance sharing</li> <li>Instance ID ownership prevents cross-instance modifications</li> <li>On read, local cache is checked first, then Dapr state store</li> </ul>"},{"location":"architecture/#key-design-decisions","title":"Key Design Decisions","text":""},{"location":"architecture/#instance-ownership","title":"Instance Ownership","text":"<p>Connections are owned by specific service instances (identified by <code>InstanceIdProvider</code>). Before any modification, the service verifies it owns the connection. This prevents race conditions in distributed deployments.</p> <p>Instance ID: Uses the <code>HOSTNAME</code> environment variable (Kubernetes pod name) to uniquely identify each instance. If <code>HOSTNAME</code> is not set, a random GUID is generated.</p> <p>Ownership check: Every mutation operation (disconnect, start/stop publish, send) verifies that <code>connection.InstanceId == instanceIdProvider.GetInstanceId()</code>. If the instance IDs don't match, a <code>ConnectionOwnershipException</code> is thrown.</p>"},{"location":"architecture/#rolling-update-behavior","title":"Rolling Update Behavior","text":"<p>When pods are replaced via rolling update:</p> <ol> <li>The new pod starts with a new <code>HOSTNAME</code> (new instance ID)</li> <li>When a pod dies, orphaned connections/workers are lazily reclaimed    on the next RPC targeting that resource \u2014 the surviving instance    re-stamps the state-store record and retries locally.</li> <li>For each connection owned by an instance not in the known-live set, and older than 60 seconds, the connection is re-claimed by the new instance</li> <li>Re-claimed connections have their <code>InstanceId</code> updated in the Dapr state store</li> </ol> <p><code>CLEAR_STALE_ON_START</code> flag: Set this environment variable to <code>TRUE</code> to clear all connections from the state store on startup. Use this when you prefer to start with a clean slate rather than recover orphaned connections.</p>"},{"location":"architecture/#two-tier-storage","title":"Two-Tier Storage","text":"<p>The <code>DistributedWebSocketConnectionStore</code> maintains both: - Local in-memory cache (fast access) - Dapr state store (distributed persistence)</p> <p>This ensures minimal latency for local operations while supporting distributed scenarios.</p>"},{"location":"architecture/#correlated-request-response","title":"Correlated Request-Response","text":"<p><code>SendAndWaitAsync</code> wraps messages with correlation IDs, enabling correlation between requests and responses without modifying external WebSocket protocols.</p>"},{"location":"architecture/#auto-reconnect","title":"Auto-Reconnect","text":"<p>The <code>WebSocketClientWrapper</code> implements exponential backoff reconnection, configurable via <code>WebSocketOptions.ReconnectMaxAttempts</code> and <code>ReconnectBaseDelayMs</code>.</p>"},{"location":"architecture/#dependency-injection-setup","title":"Dependency Injection Setup","text":"<p>All services are registered as singletons in <code>Program.cs</code>:</p> <pre><code>builder.Services.AddSingleton&lt;IWebSocketConnectionStore, DistributedWebSocketConnectionStore&gt;();\nbuilder.Services.AddSingleton&lt;IWebSocketClientWrapper, WebSocketClientWrapper&gt;();\nbuilder.Services.AddSingleton&lt;IWebSocketService, WebSocketService&gt;();\nbuilder.Services.AddSingleton&lt;IDaprPublisher, DaprPublisher&gt;();\nbuilder.Services.AddSingleton&lt;IDaprConnectionRepository, DaprConnectionRepository&gt;();\nbuilder.Services.AddSingleton&lt;IInstanceIdProvider, DefaultInstanceIdProvider&gt;();\n</code></pre> <p>The <code>DaprClient</code> is configured with custom HTTP and gRPC endpoints based on <code>PortConstants.DaprHttpPortKey</code> and <code>PortConstants.DaprGrpcPortKey</code>.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>Requirements: Dapr 1.18+ with mTLS enabled. .NET 10.0 SDK.</p> <p>WebSocketManager supports configuration via environment variables and command-line arguments. Both methods are supported with consistent behavior across all Virtufin projects.</p>"},{"location":"configuration/#precedence","title":"Precedence","text":"<p>Configuration is loaded in the following order:</p> <ol> <li>Environment variables (loaded first)</li> <li>Command-line arguments (loaded second)</li> </ol> <p>Command-line arguments take precedence over environment variables.</p>"},{"location":"configuration/#configuration-keys","title":"Configuration Keys","text":""},{"location":"configuration/#port-configuration","title":"Port Configuration","text":"Key Description Default <code>HttpPort</code> HTTP port (REST, Swagger, health) <code>5001</code> <code>GrpcPort</code> gRPC port (native gRPC HTTP/2 h2c) <code>5002</code> <code>DaprHttpPort</code> Dapr HTTP port <code>3500</code> <code>DaprGrpcPort</code> Dapr gRPC port <code>50001</code>"},{"location":"configuration/#logging","title":"Logging","text":"Key Description Default <code>Logging:LogLevel:Default</code> Minimum log level <code>Information</code> <p>Valid log levels: <code>Trace</code>, <code>Debug</code>, <code>Information</code>, <code>Warning</code>, <code>Error</code>, <code>Critical</code>, <code>None</code></p>"},{"location":"configuration/#dapr-configuration","title":"Dapr Configuration","text":"Key Description Default <code>pubsubName</code> Pub/sub component name <code>pubsub</code> <code>stateStoreName</code> State store component name <code>statestore</code>"},{"location":"configuration/#connection-record-ttl","title":"Connection record TTL","text":"Key Description Default Range <code>CONNECTION_RECORD_TTL_SECONDS</code> Dapr state TTL on connection records. A background connection heartbeat (distinct from the peer-discovery heartbeat used for RPC forwarding, see Services) refreshes every locally-owned, connected record every TTL/3 seconds (floored at 10s), independent of <code>Send</code>/<code>SendRaw</code> traffic. <code>Send</code>/<code>SendRaw</code> also refresh it as a side effect. A pod that dies without a clean disconnect stops refreshing its connections' records, which then expire and are removed by the state store. <code>300</code> <code>30</code>\u2013<code>3600</code>"},{"location":"configuration/#environment-variables","title":"Environment Variables","text":"<p>Set configuration using environment variables:</p> <pre><code>export HttpPort=8080\nexport GrpcPort=5002\nexport DaprHttpPort=3500\nexport DaprGrpcPort=50001\nexport Logging:LogLevel:Default=Debug\nexport pubsubName=my-pubsub\nexport stateStoreName=my-state\n</code></pre>"},{"location":"configuration/#command-line-arguments","title":"Command-Line Arguments","text":"<p>Pass arguments when running the application:</p> <pre><code>Virtufin.WebSocketManager --http-port 8080 --grpc-port 5002\n</code></pre>"},{"location":"configuration/#available-arguments","title":"Available Arguments","text":"Argument Short Description Default <code>--http-port &lt;port&gt;</code> <code>-s</code> HTTP port (REST, Swagger, health) <code>5001</code> <code>--grpc-port &lt;port&gt;</code> <code>-g</code> gRPC port (native gRPC HTTP/2 h2c) <code>5002</code> <code>--dapr-http-port &lt;port&gt;</code> Dapr HTTP port <code>3500</code> <code>--dapr-grpc-port &lt;port&gt;</code> Dapr gRPC port <code>50001</code> <code>--log-level &lt;level&gt;</code> <code>-l</code> Minimum log level <code>Information</code> <code>--pubsub-name &lt;name&gt;</code> <code>-n</code> Pub/sub component name <code>pubsub</code> <code>--state-store-name &lt;name&gt;</code> <code>-t</code> State store component name <code>statestore</code> <code>--help</code> <code>-?</code> Show help message"},{"location":"configuration/#examples","title":"Examples","text":""},{"location":"configuration/#using-environment-variables","title":"Using Environment Variables","text":"<pre><code>export HttpPort=3000\nexport GrpcPort=3002\n./Virtufin.WebSocketManager\n</code></pre>"},{"location":"configuration/#using-command-line-arguments","title":"Using Command-Line Arguments","text":"<pre><code>./Virtufin.WebSocketManager --http-port 3000 --grpc-port 3002 --log-level Debug\n</code></pre>"},{"location":"configuration/#combining-both","title":"Combining Both","text":"<p>Environment variables set defaults, command-line arguments override:</p> <pre><code>export HttpPort=3000\n./Virtufin.WebSocketManager --grpc-port 3002\n</code></pre> <p>In this example, <code>HttpPort</code> is <code>3000</code> (from environment) and <code>GrpcPort</code> is <code>3002</code> (from command line).</p>"},{"location":"configuration/#validation","title":"Validation","text":"<p>The application validates port values on startup:</p> <ul> <li>Ports must be between 1 and 65535</li> </ul> <p>If validation fails, the application writes an error message to stderr and exits with code 1.</p>"},{"location":"connection-lifecycle/","title":"WebSocket Connection Lifecycle","text":"<p>This document describes the connection lifecycle events published to the <code>websocketmanager.lifecycle</code> topic by the WebSocketManager.</p>"},{"location":"connection-lifecycle/#event-types","title":"Event Types","text":"Type When Data <code>connection.connected</code> Connection established with the upstream WS server <code>connection_id</code>, <code>url</code>, <code>instance_id</code>, <code>reason</code> (previous status) <code>connection.disconnected</code> Connection closed (any reason) <code>connection_id</code>, <code>url</code>, <code>instance_id</code>, <code>reason</code> (previous status) <code>connection.reconnecting</code> Auto-reconnect attempt in progress <code>connection_id</code>, <code>url</code>, <code>instance_id</code>, <code>reason</code> (previous status), <code>attempt</code> (reconnect attempt number) <code>connection.error</code> Receive loop caught an unexpected exception <code>connection_id</code>, <code>url</code>, <code>instance_id</code>, <code>error_type</code>, <code>error_message</code> <p><code>attempt</code> is currently always <code>1</code>: the WSM fires exactly one <code>connection.reconnecting</code> event before entering its retry loop, not one per attempt, so the field reflects the upcoming first attempt rather than a live counter.</p> <p>All events are CloudEvents v1.0 envelopes. Subscribe via <code>Pubsub.Subscribe(topic: Topics.Lifecycle)</code> on the virtufin-api; subscribers from outside the WSM process can hardcode <code>\"websocketmanager.lifecycle\"</code> (the string source of truth is <code>virtufin-websocketmanager/src/Virtufin.WebSocketManager/Configuration/Topics.cs:Lifecycle</code>).</p>"},{"location":"connection-lifecycle/#scenarios","title":"Scenarios","text":""},{"location":"connection-lifecycle/#scenario-1-server-initiates-close-eg-binance-terminates-the-connection","title":"Scenario 1: Server initiates close (e.g. Binance terminates the connection)","text":"<p>The WebSocket server sends a Close frame. The wrapper detects it and:</p> <ul> <li>If <code>auto_reconnect = true</code>: fires <code>connection.reconnecting</code>, then either   <code>connection.connected</code> (on successful reconnect) or   <code>connection.disconnected</code> (after <code>ReconnectMaxAttempts</code> is exhausted).</li> <li>If <code>auto_reconnect = false</code>: fires <code>connection.disconnected</code>.</li> </ul> <p>Consumer guidance: treat <code>connection.reconnecting</code> as a transient signal. Wait for <code>connection.connected</code> or <code>connection.disconnected</code> before taking corrective action. The backoff is exponential, capped at <code>ReconnectBaseDelayMs</code>.</p>"},{"location":"connection-lifecycle/#scenario-2-network-error-disconnects-the-connection","title":"Scenario 2: Network error disconnects the connection","text":"<p>The receive loop catches an exception (<code>WebSocketException</code>, <code>IOException</code>, etc.). The wrapper fires <code>connection.error</code> FIRST (with the exception context), then follows the same path as Scenario 1: <code>connection.reconnecting</code> (or <code>connection.disconnected</code>) \u2192 final state.</p> <p>Consumer guidance: <code>connection.error</code> provides diagnostic context (exception type, message). It always precedes a state transition, so subscribe to it for monitoring but always handle the subsequent <code>connection.reconnecting</code> / <code>connection.disconnected</code> for the actual recovery action.</p>"},{"location":"connection-lifecycle/#scenario-3-user-initiated-disconnect","title":"Scenario 3: User-initiated disconnect","text":"<p>The user calls <code>Disconnect</code> on the worker. The wrapper fires <code>connection.disconnected</code> only (no <code>connection.reconnecting</code>).</p>"},{"location":"connection-lifecycle/#scenario-4-initial-connect","title":"Scenario 4: Initial connect","text":"<p>The user calls <code>Create</code> then issues a <code>connect</code> command. The wrapper fires <code>connection.connected</code> once the upstream WebSocket handshake completes.</p>"},{"location":"connection-lifecycle/#event-ordering-guarantees","title":"Event Ordering Guarantees","text":"<ul> <li><code>connection.error</code> is always followed by either   <code>connection.reconnecting</code> or <code>connection.disconnected</code>.</li> <li><code>connection.reconnecting</code> is always followed by either   <code>connection.connected</code> (success) or <code>connection.disconnected</code>   (exhausted).</li> <li>Events for the same <code>connection_id</code> are delivered in the order they   occur in the wrapper.</li> </ul>"},{"location":"connection-lifecycle/#best-effort-delivery","title":"Best-Effort Delivery","text":"<p>The lifecycle publisher is best-effort. If the virtufin-api is unreachable, the wrapper logs a warning and continues. The connection's state is still updated locally; only the cross-service event is dropped.</p>"},{"location":"csharp-client/","title":"C# Client Library","text":"<p>The Virtufin WebSocketManager Client library (<code>Virtufin.WebSocketManager.Client</code>) provides a C# interface for managing external WebSocket connections.</p>"},{"location":"csharp-client/#installation","title":"Installation","text":""},{"location":"csharp-client/#from-gitea-nuget-registry","title":"From Gitea NuGet Registry","text":"<p>Add the Virtufin package source with a personal access token (scope: <code>read:packages</code>):</p> <pre><code>dotnet nuget add source https://nuget.haenerconsulting.com/api/packages/virtufin/nuget/index.json \\\n  --name Virtufin --username &lt;your-gitea-username&gt; --password &lt;your-gitea-token&gt;\n\ndotnet add package Virtufin.WebSocketManager.Client\n</code></pre> <p>Or configure via <code>NuGet.Config</code>:</p> <pre><code>&lt;configuration&gt;\n  &lt;packageSources&gt;\n    &lt;add key=\"Virtufin\" value=\"https://nuget.haenerconsulting.com/api/packages/virtufin/nuget/index.json\" /&gt;\n  &lt;/packageSources&gt;\n  &lt;packageSourceCredentials&gt;\n    &lt;Virtufin&gt;\n      &lt;add key=\"Username\" value=\"&lt;your-gitea-username&gt;\" /&gt;\n      &lt;add key=\"ClearTextPassword\" value=\"&lt;your-gitea-token&gt;\" /&gt;\n    &lt;/Virtufin&gt;\n  &lt;/packageSourceCredentials&gt;\n&lt;/configuration&gt;\n</code></pre>"},{"location":"csharp-client/#from-local-source","title":"From Local Source","text":"<pre><code>&lt;ProjectReference Include=\"path/to/Virtufin.WebSocketManager.Client/Virtufin.WebSocketManager.Client.csproj\" /&gt;\n</code></pre>"},{"location":"csharp-client/#overview","title":"Overview","text":"<p>The client library provides the <code>WebSocketManagerClient</code> class for connecting to external WebSocket servers through the WebSocketManager service.</p>"},{"location":"csharp-client/#quick-start","title":"Quick Start","text":"<pre><code>using Virtufin.WebSocketManager.Client;\nusing Virtufin.WebSocketManager.Protos;\n\nusing var client = new WebSocketManagerClient(\"localhost\", 5002);\n\n// Connect to a WebSocket server\nvar connectResponse = await client.ConnectAsync(\n    new ConnectRequest { Url = \"wss://example.com/socket\", AutoReconnect = true });\nConsole.WriteLine($\"Connected: {connectResponse.Id} - {connectResponse.Status}\");\n\n// List all connections\nvar listResponse = await client.ListAsync(new ListRequest());\nforeach (var conn in listResponse.Connections)\n    Console.WriteLine($\"{conn.Id}: {conn.Url} [{conn.Status}]\");\n\n// Send a message and wait for response\nvar sendResponse = await client.SendAsync(new SendRequest {\n    Id = connectResponse.Id,\n    Message = \"{\\\"action\\\": \\\"ping\\\"}\",\n    TimeoutMs = 5000\n});\nConsole.WriteLine($\"Response: {sendResponse.Response}\");\n\n// Disconnect\nawait client.DisconnectAsync(new DisconnectRequest { Id = connectResponse.Id });\n</code></pre>"},{"location":"csharp-client/#websocketmanagerclient","title":"WebSocketManagerClient","text":"<p>The main client class for WebSocket connection management.</p>"},{"location":"csharp-client/#constructor","title":"Constructor","text":"<pre><code>public WebSocketManagerClient(string host = \"localhost\", int port = 5002)\n</code></pre> <p>Parameters: - <code>host</code> - The gRPC host address (default: <code>\"localhost\"</code>) - <code>port</code> - The gRPC port (default: <code>5002</code>)</p>"},{"location":"csharp-client/#properties","title":"Properties","text":"<pre><code>// Direct access to the underlying gRPC client\npublic WebSocketManager.WebSocketManagerClient GrpcClient { get; }\n</code></pre>"},{"location":"csharp-client/#methods","title":"Methods","text":""},{"location":"csharp-client/#connection-management","title":"Connection Management","text":"<pre><code>// Connect to a WebSocket server\nTask&lt;ConnectResponse&gt; ConnectAsync(ConnectRequest request)\n\n// Disconnect from a WebSocket server\nTask&lt;DisconnectResponse&gt; DisconnectAsync(DisconnectRequest request)\n\n// List all managed connections\nTask&lt;ListResponse&gt; ListAsync(ListRequest request)\n</code></pre> <p>Example: <pre><code>var response = await client.ConnectAsync(new ConnectRequest {\n    Url = \"wss://example.com/socket\",\n    AutoReconnect = true\n});\nConsole.WriteLine($\"Connected: {response.Id}\");\n\nvar connections = await client.ListAsync(new ListRequest());\nforeach (var c in connections.Connections)\n    Console.WriteLine($\"{c.Id}: {c.Url} [{c.Status}]\");\n\nawait client.DisconnectAsync(new DisconnectRequest { Id = response.Id });\n</code></pre></p>"},{"location":"csharp-client/#pubsub-integration","title":"Pub/Sub Integration","text":"<pre><code>// Start publishing WebSocket messages to a Dapr pub/sub topic\nTask&lt;StartPublishResponse&gt; StartPublishAsync(StartPublishRequest request)\n\n// Stop publishing to the topic\nTask&lt;StopPublishResponse&gt; StopPublishAsync(StopPublishRequest request)\n</code></pre> <p>Example: <pre><code>await client.StartPublishAsync(new StartPublishRequest {\n    Id = connectionId,\n    Topic = \"websocket-events\"\n});\n\nawait client.StopPublishAsync(new StopPublishRequest { Id = connectionId });\n</code></pre></p>"},{"location":"csharp-client/#messaging","title":"Messaging","text":"<pre><code>// Send a message and wait for a correlated response\nTask&lt;SendResponse&gt; SendAsync(SendRequest request)\n\n// Send a message without waiting for a response\nTask&lt;SendRawResponse&gt; SendRawAsync(SendRawRequest request)\n</code></pre> <p>Example: <pre><code>// Send with correlation (response expected)\nvar response = await client.SendAsync(new SendRequest {\n    Id = connectionId,\n    Message = \"{\\\"type\\\": \\\"request\\\", \\\"id\\\": \\\"123\\\"}\",\n    TimeoutMs = 5000\n});\nConsole.WriteLine($\"Got response: {response.Response}\");\n\n// Fire-and-forget\nawait client.SendRawAsync(new SendRawRequest {\n    Id = connectionId,\n    Message = \"{\\\"type\\\": \\\"notification\\\"}\"\n});\n</code></pre></p>"},{"location":"csharp-client/#tags","title":"Tags","text":"<pre><code>// Set/replace a single tag on a connection (resource metadata \u2014 not forwarded to remote WS)\nTask&lt;SetTagResponse&gt; SetTagAsync(string id, string key, string value)\n\n// Get a single tag value\nTask&lt;GetTagResponse&gt; GetTagAsync(string id, string key)\n\n// Replace all tags on a connection at once\nTask&lt;SetTagsResponse&gt; SetTagsAsync(string id, IDictionary&lt;string, string&gt; tags)\n\n// Get all tags for a connection\nTask&lt;GetTagsResponse&gt; GetTagsAsync(string id)\n</code></pre> <p>Example: <pre><code>// Set a single tag\nawait client.SetTagAsync(connectionId, \"environment\", \"staging\");\n\n// Replace all tags\nawait client.SetTagsAsync(connectionId, new Dictionary&lt;string, string&gt;\n{\n    [\"environment\"] = \"staging\",\n    [\"team\"] = \"payments\"\n});\n\n// Read back\nvar tags = await client.GetTagsAsync(connectionId);\nConsole.WriteLine($\"Tags: {string.Join(\", \", tags.Select(kv =&gt; $\"{kv.Key}={kv.Value}\"))}\");\n\n// Filter connections by tag via ListRequest.Filters\nvar listRequest = new ListRequest();\nlistRequest.Filters.Add(new TagFilter { Key = \"environment\", Value = \"staging\" });\nvar matching = await client.ListAsync(listRequest);\n</code></pre></p>"},{"location":"csharp-client/#data-models","title":"Data Models","text":""},{"location":"csharp-client/#connectrequest","title":"ConnectRequest","text":"<pre><code>message ConnectRequest {\n    string Url = 1;\n    bool AutoReconnect = 2;\n}\n</code></pre>"},{"location":"csharp-client/#connectresponse","title":"ConnectResponse","text":"<pre><code>message ConnectResponse {\n    string Id = 1;\n    string Status = 2;\n}\n</code></pre>"},{"location":"csharp-client/#websocketconnection","title":"WebSocketConnection","text":"<pre><code>message WebSocketConnection {\n    string Id = 1;\n    string Url = 2;\n    string Status = 3;\n    string Topic = 4;\n    string InstanceId = 5;\n}\n</code></pre>"},{"location":"csharp-client/#sendrequest","title":"SendRequest","text":"<pre><code>message SendRequest {\n    string Id = 1;\n    string Message = 2;\n    int32 TimeoutMs = 3;\n}\n</code></pre>"},{"location":"csharp-client/#sendresponse","title":"SendResponse","text":"<pre><code>message SendResponse {\n    string Response = 1;\n}\n</code></pre>"},{"location":"csharp-client/#error-handling","title":"Error Handling","text":""},{"location":"csharp-client/#connection-not-found","title":"Connection Not Found","text":"<pre><code>try {\n    await client.DisconnectAsync(new DisconnectRequest { Id = \"nonexistent\" });\n}\ncatch (Grpc.Core.RpcException ex) when (ex.StatusCode == StatusCode.NotFound)\n{\n    Console.WriteLine(\"Connection not found\");\n}\n</code></pre>"},{"location":"csharp-client/#send-timeout","title":"Send Timeout","text":"<pre><code>try {\n    var response = await client.SendAsync(new SendRequest {\n        Id = connectionId,\n        Message = \"{\\\"action\\\": \\\"slow\\\"}\",\n        TimeoutMs = 100 // very short timeout\n    });\n}\ncatch (Grpc.Core.RpcException ex) when (ex.StatusCode == StatusCode.DeadlineExceeded)\n{\n    Console.WriteLine(\"Send timed out\");\n}\n</code></pre>"},{"location":"csharp-client/#connection-errors","title":"Connection Errors","text":"<pre><code>try {\n    using var client = new WebSocketManagerClient(\"unreachable-host\", 5001);\n    var response = await client.ConnectAsync(new ConnectRequest { Url = \"wss://invalid\" });\n}\ncatch (Grpc.Core.RpcException ex)\n{\n    Console.WriteLine($\"Connection failed: {ex.Status}\");\n}\n</code></pre>"},{"location":"csharp-client/#complete-example","title":"Complete Example","text":"<pre><code>using Grpc.Core;\nusing Virtufin.WebSocketManager.Client;\nusing Virtufin.WebSocketManager.Protos;\n\nclass Program\n{\n    static async Task Main(string[] args)\n    {\n        Console.WriteLine(\"WebSocketManager Client Demo\");\n        Console.WriteLine(\"==============================\\n\");\n\nusing var client = new WebSocketManagerClient(\"localhost\", 5001);\n\n        // Connect\n        Console.WriteLine(\"Connecting to WebSocket server...\");\n        var connectResponse = await client.ConnectAsync(new ConnectRequest {\n            Url = \"wss://echo.websocket.org\",\n            AutoReconnect = false\n        });\n        Console.WriteLine($\"Connected: {connectResponse.Id} - {connectResponse.Status}\");\n\n        // List connections\n        Console.WriteLine(\"\\nAll connections:\");\n        var listResponse = await client.ListAsync(new ListRequest());\n        foreach (var c in listResponse.Connections) {\n            Console.WriteLine($\"  {c.Id}: {c.Url} [{c.Status}]\");\n            if (!string.IsNullOrEmpty(c.Topic))\n                Console.WriteLine($\"    Publishing to: {c.Topic}\");\n        }\n\n        // Send a message\n        Console.WriteLine(\"\\nSending message...\");\n        var sendResponse = await client.SendAsync(new SendRequest {\n            Id = connectResponse.Id,\n            Message = \"Hello, WebSocket server!\",\n            TimeoutMs = 5000\n        });\n        Console.WriteLine($\"Response: {sendResponse.Response}\");\n\n        // Start pub/sub\n        Console.WriteLine(\"\\nStarting pub/sub...\");\n        await client.StartPublishAsync(new StartPublishRequest {\n            Id = connectResponse.Id,\n            Topic = \"ws-events\"\n        });\n        Console.WriteLine(\"Publishing started\");\n\n        // Stop pub/sub\n        Console.WriteLine(\"\\nStopping pub/sub...\");\n        await client.StopPublishAsync(new StopPublishRequest { Id = connectResponse.Id });\n        Console.WriteLine(\"Publishing stopped\");\n\n        // Disconnect\n        Console.WriteLine(\"\\nDisconnecting...\");\n        await client.DisconnectAsync(new DisconnectRequest { Id = connectResponse.Id });\n        Console.WriteLine(\"Disconnected\");\n    }\n}\n</code></pre>"},{"location":"csharp-client/#protobuf-dependencies","title":"Protobuf Dependencies","text":"<p>The client library requires:</p> <pre><code>&lt;PackageReference Include=\"Google.Protobuf\" /&gt;\n&lt;PackageReference Include=\"Grpc.Net.Client\" /&gt;\n&lt;PackageReference Include=\"Grpc.Tools\" /&gt;\n</code></pre> <p>The proto file (<code>websocketmanager.proto</code>) is compiled into: - <code>Virtufin.WebSocketManager.Protos.WebSocketManager</code> - <code>Virtufin.WebSocketManager.Protos.WebSocketManagerClient</code> - Request/Response message classes</p>"},{"location":"csharp-client/#sdk-reference","title":"SDK Reference","text":"<ul> <li>C# API Reference</li> <li>Python API Reference</li> <li>TypeScript API Reference</li> </ul>"},{"location":"deployment/","title":"Deployment","text":""},{"location":"deployment/#docker","title":"Docker","text":"<p>The project includes a multi-stage Dockerfile for production deployment.</p>"},{"location":"deployment/#build-the-image","title":"Build the Image","text":"<pre><code>docker build \\\n  --build-arg DOTNET_VERSION=10.0 \\\n  -t virtufin-websocketmanager \\\n  -f src/Virtufin.WebSocketManager/Dockerfile \\\n  .\n</code></pre>"},{"location":"deployment/#run-with-dapr","title":"Run with Dapr","text":"<pre><code>docker run \\\n  -p 5001:5001 \\\n  -p 5002:5002 \\\n  virtufin-websocketmanager\n</code></pre>"},{"location":"deployment/#configuration","title":"Configuration","text":"<p>All settings are configured via <code>appsettings.json</code> with environment variable overrides. See Configuration for all available options.</p>"},{"location":"deployment/#environment-variables","title":"Environment Variables","text":"Variable Default Description <code>HttpPort</code> <code>5001</code> HTTP port (REST, Swagger, health) <code>GrpcPort</code> <code>5002</code> gRPC port (native gRPC h2c) <code>DaprHttpPort</code> <code>3500</code> Dapr sidecar HTTP port <code>DaprGrpcPort</code> <code>50001</code> Dapr sidecar gRPC port <code>Logging__LogLevel__Default</code> <code>Information</code> Minimum log level (env var override for <code>Logging:LogLevel:Default</code>)"},{"location":"deployment/#health-checks","title":"Health Checks","text":"<p>The service exposes health check endpoints:</p> <pre><code># HTTP/gRPC health check\ncurl http://localhost:5001/health\n</code></pre>"},{"location":"deployment/#monitoring","title":"Monitoring","text":"<p>The service emits structured logs with OpenTelemetry trace context. Configure your observability backend in <code>appsettings.json</code>:</p> <pre><code>{\n  \"Logging\": {\n    \"LogLevel\": {\n      \"Default\": \"Information\",\n      \"Microsoft.AspNetCore\": \"Warning\"\n    }\n  }\n}\n</code></pre>"},{"location":"deployment/#known-limitations","title":"Known Limitations","text":""},{"location":"deployment/#dapr-grpc-app-health-checks-not-supported","title":"Dapr gRPC App Health Checks Not Supported","text":"<p>When using Dapr with <code>app-protocol: grpc</code>, the Dapr sidecar probes the application via the <code>dapr.proto.runtime.v1.AppCallbackHealthCheck</code> gRPC service. The Dapr .NET SDK does not currently provide a server-side implementation of this service. As a result, Dapr app health checks are disabled (<code>dapr.io/enable-app-health-check: false</code>).</p> <p>Kubernetes liveness and readiness probes (<code>/health</code> endpoint) continue to handle pod health monitoring. Future improvement: implement the Dapr AppCallback gRPC service to enable Dapr's soft health check mechanism for pausing work delivery during unhealthy states.</p>"},{"location":"development/","title":"Development Guide","text":"<p>Guide for building, testing, and running the Virtufin WebSocketManager locally.</p>"},{"location":"development/#table-of-contents","title":"Table of Contents","text":"<ul> <li>Prerequisites</li> <li>Project Structure</li> <li>Building</li> <li>Running Locally</li> <li>Testing</li> <li>Debugging</li> <li>Generating Protobuf Files</li> <li>Troubleshooting</li> </ul>"},{"location":"development/#prerequisites","title":"Prerequisites","text":""},{"location":"development/#required-software","title":"Required Software","text":"Software Version Purpose .NET SDK 10.0+ Runtime and build Dapr CLI 1.18+ Local development sidecar Docker Latest Redis/Valkey for state + pub/sub buf 1.40+ Protobuf code generation (optional)"},{"location":"development/#install-net-sdk","title":"Install .NET SDK","text":"<pre><code># macOS\nbrew install dotnet\n\n# Linux (Ubuntu)\nwget https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb\nsudo dpkg -i packages-microsoft-prod.deb\nsudo apt update\nsudo apt install dotnet-sdk-10.0\n\n# Windows\nwinget install Microsoft.DotNet.SDK.10\n</code></pre>"},{"location":"development/#install-dapr-cli","title":"Install Dapr CLI","text":"<pre><code># macOS\nbrew install dapr/tap/dapr\n\n# Linux\ncurl -fsSL https://raw.githubusercontent.com/dapr/cli/master/install/install.sh | bash\n\n# Initialize Dapr (downloads placement, redis-placement, and zipkin containers)\ndapr init\n</code></pre>"},{"location":"development/#project-structure","title":"Project Structure","text":"<pre><code>src/\n\u251c\u2500\u2500 Virtufin.WebSocketManager/             # Main application\n\u2502   \u251c\u2500\u2500 Configuration/                     # Options + PortConstants\n\u2502   \u251c\u2500\u2500 Services/                          # gRPC service + Dapr + store\n\u2502   \u2514\u2500\u2500 Program.cs\n\u251c\u2500\u2500 Virtufin.WebSocketManager.Protos/      # Proto definitions\n\u2502   \u251c\u2500\u2500 proto/websocketmanager.proto\n\u2502   \u2514\u2500\u2500 Generated/                         # Buf-generated stubs\n\u2514\u2500\u2500 Virtufin.WebSocketManager.Client/      # .NET client NuGet library\n\ntests/\n\u251c\u2500\u2500 Virtufin.WebSocketManager.Tests/       # Service-level tests\n\u251c\u2500\u2500 Virtufin.WebSocketManager.Client.Tests/# Client library tests\n\u251c\u2500\u2500 python/                                # Python client tests\n\u2514\u2500\u2500 typescript/                            # TypeScript client tests\n</code></pre>"},{"location":"development/#building","title":"Building","text":"<pre><code># Build the entire solution\ndotnet build Virtufin.WebSocketManager.slnx\n\n# Build a specific project\ndotnet build src/Virtufin.WebSocketManager/Virtufin.WebSocketManager.csproj\n</code></pre> <p>The build output is placed in <code>src/*/bin/Debug/net10.0/</code>.</p>"},{"location":"development/#running-locally","title":"Running Locally","text":""},{"location":"development/#1-start-dapr-and-redis","title":"1. Start Dapr and Redis","text":"<pre><code>dapr init   # one-time\ndocker compose up -d redis\n</code></pre>"},{"location":"development/#2-run-the-websocketmanager","title":"2. Run the WebSocketManager","text":"<p>Clone the upstream <code>virtufin/deploy-local</code> repo (or copy its <code>components/</code> directory to any path of your choice), then run with <code>--resources-path</code> pointing at that <code>components/</code> directory:</p> <pre><code>cd src/Virtufin.WebSocketManager\nDAPR_COMPONENTS_PATH=/path/to/deploy-local/components  # set to your path\ndapr run --app-id websocketmanager \\\n  --app-protocol grpc \\\n  --app-port 5002 \\\n  --dapr-http-port 3500 \\\n  --resources-path \"$DAPR_COMPONENTS_PATH\" \\\n  -- dotnet run\n</code></pre> <p>For convenience, if you cloned <code>deploy-local</code> as a sibling of <code>virtufin-websocketmanager/</code>, the path becomes <code>../deploy-local/components</code>.</p> <p>The service exposes:</p> <ul> <li>gRPC on <code>:5002</code> (with reflection enabled)</li> <li>HTTP/REST + Swagger on <code>:5001</code></li> </ul>"},{"location":"development/#3-verify","title":"3. Verify","text":"<pre><code># Health check\ncurl http://localhost:5001/health\n\n# Swagger UI\nopen http://localhost:5001/swagger\n\n# gRPC reflection service list\ngrpcurl -plaintext localhost:5002 list\n</code></pre>"},{"location":"development/#testing","title":"Testing","text":""},{"location":"development/#run-all-tests","title":"Run All Tests","text":"<pre><code>dotnet test Virtufin.WebSocketManager.slnx\n</code></pre>"},{"location":"development/#run-tests-with-coverage","title":"Run Tests with Coverage","text":"<pre><code>dotnet test Virtufin.WebSocketManager.slnx \\\n  --collect:\"XPlat Code Coverage\" \\\n  --results-directory ./TestResults\n</code></pre>"},{"location":"development/#run-specific-tests","title":"Run Specific Tests","text":"<pre><code># By class name\ndotnet test --filter \"FullyQualifiedName~DaprConnectionRepositoryTests\"\n\n# By trait/category\ndotnet test --filter \"Category=Integration\"\n</code></pre>"},{"location":"development/#test-projects","title":"Test Projects","text":"Test Project Description <code>Virtufin.WebSocketManager.Tests</code> Service-level tests (gRPC service, Dapr repository, hosted services) <code>Virtufin.WebSocketManager.Client.Tests</code> Client library tests (hand-written + generated) <code>tests/python/</code> Python client smoke tests <code>tests/typescript/</code> TypeScript client smoke tests"},{"location":"development/#writing-tests","title":"Writing Tests","text":"<ul> <li>Place unit tests next to the class they test in <code>*.Tests/</code></li> <li>Mock external dependencies (Dapr, gRPC channels) using <code>Moq</code> or hand-rolled fakes</li> <li>Use <code>[Fact]</code> (xUnit) for single-case tests, <code>[Theory]</code> with <code>[InlineData]</code> for parameterized tests</li> </ul>"},{"location":"development/#debugging","title":"Debugging","text":""},{"location":"development/#visual-studio-code","title":"Visual Studio Code","text":"<p><code>.vscode/launch.json</code> includes a debug profile that runs the WebSocketManager under Dapr:</p> <pre><code># In VS Code\ncode .\n# Press F5 \u2192 \"Launch WebSocketManager (Dapr)\" profile\n</code></pre>"},{"location":"development/#jetbrains-rider","title":"JetBrains Rider","text":"<p>Rider auto-detects the launchSettings profile. Right-click <code>Virtufin.WebSocketManager</code> \u2192 \"Run/Debug\" \u2192 choose the \"Dapr\" launch profile.</p>"},{"location":"development/#common-debug-scenarios","title":"Common Debug Scenarios","text":"<ul> <li>Dapr state not persisting: verify the Dapr sidecar logs (<code>docker logs dapr_redis-dapr_1</code>) and check the state store component config</li> </ul>"},{"location":"development/#generating-protobuf-files","title":"Generating Protobuf Files","text":"<p>Proto files live in <code>src/Virtufin.WebSocketManager.Protos/proto/</code>. To regenerate stubs after editing a <code>.proto</code> file:</p> <pre><code>cd src/Virtufin.WebSocketManager.Protos/proto\nbuf generate\n</code></pre> <p>This regenerates:</p> <ul> <li>C# stubs \u2192 <code>src/Virtufin.WebSocketManager/Generated/</code></li> <li>Python stubs \u2192 <code>src/python/virtufin/websocketmanager/websocketmanager_pb2*.py</code></li> <li>TypeScript stubs \u2192 <code>src/typescript/src/generated/websocketmanager_pb.js</code></li> </ul> <p>Generated files are excluded from git.</p>"},{"location":"development/#regenerate-client-tests","title":"Regenerate Client Tests","text":"<p>After regenerating stubs, also regenerate the cross-language client tests:</p> <pre><code># The repos are NOT a monorepo, so the script is fetched\n# directly from the virtufin-common Gitea repo at runtime.\npython3 &lt;(curl -L https://git.haenerconsulting.com/virtufin/virtufin-common/raw/branch/master/scripts/generate-client-tests.py) \\\n  src/Virtufin.WebSocketManager.Protos/proto/websocketmanager.proto \\\n  --prefix W\n</code></pre>"},{"location":"development/#troubleshooting","title":"Troubleshooting","text":""},{"location":"development/#dapr-sidecar-not-responding","title":"\"Dapr sidecar not responding\"","text":"<p>Check that Dapr is initialized and the placement service is running:</p> <pre><code>dapr status\ndocker ps | grep dapr\n</code></pre> <p>If <code>dapr_redis-placement_1</code> is not running, restart Dapr: <code>dapr init --slim</code>.</p>"},{"location":"development/#connection-not-found-errors","title":"\"Connection not found\" errors","text":"<p>This typically means the owning instance died and the connection was reclaimed by another instance. Check the reclaim logs:</p> <pre><code>docker logs virtufin-websocketmanager 2&gt;&amp;1 | grep -i reclaim\n</code></pre>"},{"location":"development/#grpc-reflection-service-not-available","title":"\"gRPC reflection service not available\"","text":"<p>Verify reflection is enabled in <code>Program.cs</code> (<code>builder.Services.AddGrpcReflection()</code>) and that you're connecting to the gRPC port (5002), not the HTTP port (5001).</p>"},{"location":"development/#port-conflicts","title":"Port conflicts","text":"<p>If 5001/5002 are in use, override via env vars:</p> <pre><code>HttpPort=5001 GrpcPort=5002 dapr run --app-port 5002 -- dotnet run\n</code></pre> <p>Or command-line switches:</p> <pre><code>dotnet run -- --http-port 6001 --grpc-port 6002\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#overview","title":"Overview","text":"<p>The Virtufin WebSocket Manager is a service for managing external WebSocket connections with both REST API and gRPC interfaces.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>.NET 8.0 SDK or later</li> <li>Dapr CLI (optional, for local development with Dapr)</li> <li>Docker (optional, for containerized deployment)</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":""},{"location":"getting-started/#clone-the-repository","title":"Clone the Repository","text":"<pre><code>git clone &lt;repository-url&gt;\ncd virtufin-websockets\n</code></pre>"},{"location":"getting-started/#build-the-project","title":"Build the Project","text":"<pre><code>dotnet restore\ndotnet build\n</code></pre>"},{"location":"getting-started/#running-the-service","title":"Running the Service","text":""},{"location":"getting-started/#default-configuration","title":"Default Configuration","text":"<p>By default, the service runs on two separate ports: - Port 5001: HTTP (REST API, Swagger UI, health checks) - Port 5002: gRPC (native gRPC HTTP/2 h2c)</p> <pre><code>dotnet run --project src/Virtufin.WebSocketManager\n</code></pre>"},{"location":"getting-started/#custom-port-configuration","title":"Custom Port Configuration","text":""},{"location":"getting-started/#command-line-arguments","title":"Command-Line Arguments","text":"<pre><code># Custom HTTP and gRPC ports\ndotnet run --project src/Virtufin.WebSocketManager -- --http-port 8080 --grpc-port 9090\n</code></pre>"},{"location":"getting-started/#environment-variables","title":"Environment Variables","text":"<pre><code>export HttpPort=4000\ndotnet run --project src/Virtufin.WebSocketManager\n</code></pre>"},{"location":"getting-started/#port-configuration-details","title":"Port Configuration Details","text":""},{"location":"getting-started/#http-port-default-5001","title":"HTTP Port (Default: 5001)","text":"<ul> <li>Uses HTTP/1.1 protocol</li> <li>Hosts REST API endpoints</li> <li>Provides Swagger UI for API documentation</li> <li>Used for health checks</li> </ul>"},{"location":"getting-started/#grpc-port-default-5002","title":"gRPC Port (Default: 5002)","text":"<ul> <li>Uses HTTP/2 (h2c) protocol</li> <li>Hosts native gRPC services</li> <li>Does NOT support gRPC-Web</li> </ul>"},{"location":"getting-started/#validation-rules","title":"Validation Rules","text":"<ul> <li>Ports must be between 1 and 65535</li> <li>Invalid configurations will display an error message</li> </ul>"},{"location":"getting-started/#api-access","title":"API Access","text":""},{"location":"getting-started/#rest-api-http-port","title":"REST API (HTTP Port)","text":"<pre><code># Health check\ncurl http://localhost:5001/health\n\n# List WebSocket connections\ncurl http://localhost:5001/websockets\n\n# Swagger UI\n# Open http://localhost:5001/swagger in your browser\n</code></pre>"},{"location":"getting-started/#grpc-api","title":"gRPC API","text":"<pre><code>// C# gRPC client example\nvar channel = GrpcChannel.ForAddress(\"http://localhost:5002\");\nvar client = new WebSocketManagerService.WebSocketManagerServiceClient(channel);\n</code></pre>"},{"location":"getting-started/#configuration-options","title":"Configuration Options","text":""},{"location":"getting-started/#log-level-control","title":"Log Level Control","text":"<pre><code>dotnet run --project src/Virtufin.WebSocketManager -- --log-level Debug\n</code></pre> <p>Available log levels: Trace, Debug, Information, Warning, Error, Critical, None</p>"},{"location":"getting-started/#dapr-integration","title":"Dapr Integration","text":"<p>The service integrates with Dapr for: - Pub/sub messaging - State management - Service discovery</p> <p>See the Dapr documentation for more information.</p>"},{"location":"getting-started/#full-stack-integration","title":"Full Stack Integration","text":"<p>The WebSocketManager is designed to work alongside the API Gateway and WorkManager. See the Cross-Service Integration Guide for a complete walkthrough covering all three services with Docker Compose and Kubernetes manifests, topic ownership, health-check sequence, and shared Dapr components.</p>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>API Documentation - Complete API reference</li> <li>Architecture Overview - System design</li> <li>Deployment Guide - Production deployment</li> <li>Security - Authentication and connection ownership</li> </ul>"},{"location":"introduction/","title":"Introduction","text":"<p>The Virtufin WebSocket Manager provides centralized WebSocket connection management with gRPC API support and Dapr integration for distributed state persistence and pub/sub messaging.</p>"},{"location":"introduction/#what-is-websocket-manager","title":"What is WebSocket Manager?","text":"<p>WebSocket Manager is a distributed service that tracks and controls WebSocket connections across multiple instances. Rather than each service managing its own WebSocket connections, the WebSocket Manager centralizes:</p> <ul> <li>Connection Lifecycle: Connect, disconnect, list, and monitor connections</li> <li>Message Routing: Send correlated request-response messages or fire-and-forget broadcasts</li> <li>Publish Control: Dynamically start and stop publishing messages to topics</li> <li>Instance Ownership: Connections are owned by specific service instances with automatic recovery</li> </ul>"},{"location":"introduction/#key-features","title":"Key Features","text":"Feature Description gRPC API Full gRPC service definition with HTTP/JSON transcoding Distributed State Dapr-backed state store for connection metadata Pub/Sub Integration Dapr pub/sub for cross-instance messaging Auto-Recovery Detect and handle connection timeouts and dead instances Correlated Messaging Request-response pattern over WebSocket with timeout support Multi-Client SDK First-class SDKs for C#, Python, and TypeScript"},{"location":"introduction/#architecture-at-a-glance","title":"Architecture at a Glance","text":"<pre><code>flowchart LR\n    SDK[C# / Python / TS Client] --&gt; GRPC[gRPC API]\n    GRPC --&gt; WS[WebSocketService]\n    WS --&gt; STORE[(Distributed Store)]\n    WS --&gt; EXT[External WebSocket Servers]\n    STORE --&gt; DAPR[Dapr Runtime]\n    DAPR --&gt; SS[(State Store)]\n    DAPR --&gt; PS[(Pub/Sub)]</code></pre>"},{"location":"introduction/#use-cases","title":"Use Cases","text":"<ul> <li>Real-Time Notifications: Push events to connected WebSocket clients</li> <li>Chat &amp; Collaboration: Manage WebSocket sessions for multi-user applications</li> <li>IoT Device Management: Track and communicate with connected devices</li> <li>Live Dashboards: Stream real-time data to browser-based dashboards</li> <li>Service Mesh: Coordinate WebSocket connections in a microservices architecture</li> </ul>"},{"location":"introduction/#getting-started","title":"Getting Started","text":"<ol> <li>Read the Architecture Overview to understand the system design</li> <li>Follow the Getting Started guide to deploy your first instance</li> <li>Explore the API Reference for detailed endpoint documentation</li> <li>Choose your SDK: C#, Python, or TypeScript</li> </ol>"},{"location":"introduction/#requirements","title":"Requirements","text":"Component Version .NET 10.0+ Dapr 1.18+ Protocol Buffers proto3"},{"location":"introduction/#next-steps","title":"Next Steps","text":"<ul> <li>Architecture \u2014 Deep dive into system design and components</li> <li>API Reference \u2014 Complete gRPC endpoint documentation</li> <li>Services \u2014 Internal service architecture</li> <li>Configuration \u2014 Runtime configuration options</li> <li>C# SDK \u2014 .NET client library</li> <li>Python SDK \u2014 Python async gRPC client</li> <li>TypeScript SDK \u2014 TypeScript Connect-RPC client</li> </ul>"},{"location":"python-client/","title":"Python Client Library","text":"<p>The Python client library provides an async gRPC interface to the WebSocketManager service.</p>"},{"location":"python-client/#installation","title":"Installation","text":""},{"location":"python-client/#from-gitea-pypi-registry","title":"From Gitea PyPI Registry","text":"<p>Set up authentication with a Gitea personal access token (scope: <code>read:packages</code>):</p> <pre><code>export PIP_USERNAME=&lt;your-gitea-username&gt;\nexport PIP_PASSWORD=&lt;your-gitea-token&gt;\n\npip install virtufin-websocketmanager \\\n  --index-url https://pypi.haenerconsulting.com/api/packages/virtufin/pypi/simple/ \\\n  --extra-index-url https://pypi.org/simple\n</code></pre>"},{"location":"python-client/#from-local-source","title":"From Local Source","text":"<pre><code>pip install grpcio grpcio-reflection\nexport PYTHONPATH=/path/to/virtufin-websocketmanager/src/python\n</code></pre>"},{"location":"python-client/#quick-start","title":"Quick Start","text":"<pre><code>import asyncio\nfrom virtufin.websocketmanager import WebSocketManagerClient\n\nasync def main():\n    async with WebSocketManagerClient(\"localhost\", 5002) as client:\n        # Connect to a WebSocket server\n        response = await client.connect(\"wss://echo.websocket.org\", auto_reconnect=True)\n        print(f\"Connected: {response.id} - {response.status}\")\n\n        # List all connections\n        list_resp = await client.list()\n        for conn in list_resp.connections:\n            print(f\"  {conn.id}: {conn.url} [{conn.status}]\")\n\n        # Send a message and wait for response\n        send_resp = await client.send(response.id, '{\"type\": \"ping\"}', timeout_ms=5000)\n        print(f\"Response: {send_resp.response}\")\n\n        # Disconnect\n        await client.disconnect(response.id)\n\nasyncio.run(main())\n</code></pre>"},{"location":"python-client/#websocketmanagerclient","title":"WebSocketManagerClient","text":"<p>The main client class for interacting with the WebSocketManager service.</p>"},{"location":"python-client/#constructor","title":"Constructor","text":"<pre><code>def __init__(self, host: str = \"localhost\", port: int = 5002)\n</code></pre> Parameter Type Default Description <code>host</code> <code>str</code> <code>\"localhost\"</code> gRPC server hostname <code>port</code> <code>int</code> <code>5002</code> gRPC server port"},{"location":"python-client/#context-manager","title":"Context Manager","text":"<pre><code>async with WebSocketManagerClient() as client:\n    # client is connected\n    pass\n# Automatically closed\n</code></pre>"},{"location":"python-client/#connection-operations","title":"Connection Operations","text":""},{"location":"python-client/#connect","title":"connect()","text":"<p>Establish a new WebSocket connection.</p> <pre><code>response: ConnectResponse = await client.connect(url: str, auto_reconnect: bool = False)\n</code></pre> Parameter Type Description <code>url</code> <code>str</code> WebSocket server URL (e.g., <code>wss://example.com/socket</code>) <code>auto_reconnect</code> <code>bool</code> Enable automatic reconnection on disconnect <p>Returns: <code>ConnectResponse</code> with <code>id</code> and <code>status</code> fields.</p> <p>Example: <pre><code>response = await client.connect(\"wss://echo.websocket.org\", auto_reconnect=True)\nprint(f\"Connected: {response.id}\")\n</code></pre></p>"},{"location":"python-client/#list","title":"list()","text":"<p>List all managed WebSocket connections.</p> <pre><code>response: ListResponse = await client.list()\n</code></pre> <p>Returns: <code>ListResponse</code> with <code>connections</code> list. Each connection has <code>id</code>, <code>url</code>, <code>status</code>, <code>topic</code>, <code>instance_id</code>.</p> <p>Example: <pre><code>resp = await client.list()\nfor conn in resp.connections:\n    print(f\"{conn.id}: {conn.url} [{conn.status}]\")\n</code></pre></p>"},{"location":"python-client/#disconnect","title":"disconnect()","text":"<p>Disconnect and remove a WebSocket connection.</p> <pre><code>await client.disconnect(id: str)\n</code></pre> Parameter Type Description <code>id</code> <code>str</code> Connection ID to disconnect <p>Example: <pre><code>await client.disconnect(connection_id)\n</code></pre></p>"},{"location":"python-client/#messaging-operations","title":"Messaging Operations","text":""},{"location":"python-client/#send","title":"send()","text":"<p>Send a message and wait for a correlated response.</p> <pre><code>response: SendResponse = await client.send(id: str, message: str, timeout_ms: int = 5000)\n</code></pre> Parameter Type Description <code>id</code> <code>str</code> Connection ID <code>message</code> <code>str</code> Message to send (JSON string) <code>timeout_ms</code> <code>int</code> Timeout in milliseconds to wait for response <p>Returns: <code>SendResponse</code> with <code>response</code> field containing the server reply.</p> <p>Example: <pre><code>resp = await client.send(connection_id, '{\"type\": \"request\", \"id\": \"123\"}', timeout_ms=5000)\nprint(f\"Response: {resp.response}\")\n</code></pre></p>"},{"location":"python-client/#send_raw","title":"send_raw()","text":"<p>Send a message without waiting for a response.</p> <pre><code>await client.send_raw(id: str, message: str)\n</code></pre> Parameter Type Description <code>id</code> <code>str</code> Connection ID <code>message</code> <code>str</code> Message to send <p>Example: <pre><code>await client.send_raw(connection_id, '{\"type\": \"notification\"}\")\n</code></pre></p>"},{"location":"python-client/#pubsub-operations","title":"Pub/Sub Operations","text":""},{"location":"python-client/#start_publish","title":"start_publish()","text":"<p>Start publishing WebSocket messages to a Dapr pub/sub topic.</p> <pre><code>await client.start_publish(id: str, topic: str)\n</code></pre> Parameter Type Description <code>id</code> <code>str</code> Connection ID <code>topic</code> <code>str</code> Dapr pub/sub topic name <p>Example: <pre><code>await client.start_publish(connection_id, \"websocket-events\")\n</code></pre></p>"},{"location":"python-client/#stop_publish","title":"stop_publish()","text":"<p>Stop publishing WebSocket messages to the topic.</p> <pre><code>await client.stop_publish(id: str)\n</code></pre> Parameter Type Description <code>id</code> <code>str</code> Connection ID <p>Example: <pre><code>await client.stop_publish(connection_id)\n</code></pre></p>"},{"location":"python-client/#set_tag","title":"set_tag()","text":"<p>Set a single tag on a connection (resource metadata \u2014 not forwarded to the remote websocket).</p> <pre><code>await client.set_tag(id: str, key: str, value: str)\n</code></pre> Parameter Type Description <code>id</code> <code>str</code> Connection ID <code>key</code> <code>str</code> Tag name <code>value</code> <code>str</code> Tag value"},{"location":"python-client/#get_tag","title":"get_tag()","text":"<p>Get a single tag value. Returns <code>None</code> if the tag is not set.</p> <pre><code>value: str | None = await client.get_tag(id: str, key: str)\n</code></pre>"},{"location":"python-client/#set_tags","title":"set_tags()","text":"<p>Replace all tags on a connection at once.</p> <pre><code>await client.set_tags(id: str, tags: dict[str, str])\n</code></pre>"},{"location":"python-client/#get_tags","title":"get_tags()","text":"<p>Get all tags for a connection.</p> <pre><code>tags: dict[str, str] = await client.get_tags(id: str)\n</code></pre> <p>Example: <pre><code># Set tags on a connection\nawait client.set_tag(conn_id, \"environment\", \"staging\")\n\n# Bulk replace\nawait client.set_tags(conn_id, {\"environment\": \"staging\", \"team\": \"payments\"})\n\n# Read back\ntags = await client.get_tags(conn_id)\nprint(f\"Tags: {tags}\")\n</code></pre></p>"},{"location":"python-client/#via-the-api-gateway","title":"Via the API Gateway","text":"<p>The <code>WebSocketManagerClient</code> connects directly to the websocketmanager's gRPC port (<code>5002</code> by default). Alternatively, you can invoke websocketmanager methods through the Virtufin API gateway using the gateway's dynamic-dispatch pattern:</p> <pre><code>from virtufin.api.client import ApiClient\n\nasync with ApiClient() as client:\n    wsm = client.gateway.websocketmanager\n\n    # List \u2014 no request fields; returns {\"connections\": [...]}\n    connections = await wsm.List()\n    for c in connections[\"connections\"]:\n        print(f\"  {c['id']}: {c['url']} ({c['status']})\")\n\n    # Connect \u2014 url, auto_reconnect\n    conn = await wsm.Connect({\n        \"url\": \"wss://stream.example.com/feed\",\n        \"auto_reconnect\": True,\n    })\n    connection_id = conn[\"id\"]\n\n    # Disconnect \u2014 id field\n    await wsm.Disconnect({\"id\": connection_id})\n</code></pre> <p>The gateway pattern is useful when: - The websocketmanager's gRPC port is not directly reachable (e.g.,   behind a service mesh, or only the gateway is exposed) - You want service discovery without configuring each backend's   address explicitly - You're writing a tool that needs to talk to any of multiple   backend services (websocketmanager, workmanager, custom)   through one entry point</p> <p>See the virtufin-api Python client docs for the full gateway client reference, and the proto-to-client-mapping spec \u00a7Layer 4 for the dynamic-dispatch call chain (gRPC reflection, JSON marshaling).</p>"},{"location":"python-client/#complete-example","title":"Complete Example","text":"<pre><code>#!/usr/bin/env python3\n\"\"\"Example: WebSocket management with the Python client.\"\"\"\n\nimport asyncio\nfrom virtufin.websocketmanager import WebSocketManagerClient\n\n\nasync def main():\n    async with WebSocketManagerClient(\"localhost\", 5002) as client:\n        # Connect\n        print(\"Connecting to WebSocket server...\")\n        connect_resp = await client.connect(\n            \"wss://echo.websocket.org\",\n            auto_reconnect=False\n        )\n        print(f\"Connected: {connect_resp.id} - {connect_resp.status}\")\n\n        # List connections\n        print(\"\\nAll connections:\")\n        list_resp = await client.list()\n        for conn in list_resp.connections:\n            print(f\"  {conn.id}: {conn.url} [{conn.status}]\")\n            if conn.topic:\n                print(f\"    Publishing to: {conn.topic}\")\n\n        # Send a message\n        print(\"\\nSending message...\")\n        send_resp = await client.send(\n            connect_resp.id,\n            '{\"type\": \"request\", \"id\": \"123\", \"data\": \"hello\"}',\n            timeout_ms=5000\n        )\n        print(f\"Response: {send_resp.response}\")\n\n        # Start pub/sub\n        print(\"\\nStarting pub/sub...\")\n        await client.start_publish(connect_resp.id, \"ws-events\")\n        print(\"Publishing started\")\n\n        # Stop pub/sub\n        print(\"\\nStopping pub/sub...\")\n        await client.stop_publish(connect_resp.id)\n        print(\"Publishing stopped\")\n\n        # Disconnect\n        print(\"\\nDisconnecting...\")\n        await client.disconnect(connect_resp.id)\n        print(\"Disconnected\")\n\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n</code></pre>"},{"location":"python-client/#error-handling","title":"Error Handling","text":"<pre><code>import grpc\n\ntry:\n    await client.connect(\"wss://invalid-host\")\nexcept grpc.aio.AioRpcError as e:\n    print(f\"gRPC error: {e.code()} - {e.details()}\")\n</code></pre> <p>Common gRPC status codes: - <code>StatusCode.NOT_FOUND</code> - Connection not found - <code>StatusCode.INVALID_ARGUMENT</code> - Invalid URL or parameters - <code>StatusCode.DEADLINE_EXCEEDED</code> - Send timed out - <code>StatusCode.UNAVAILABLE</code> - Server not reachable</p>"},{"location":"python-client/#sdk-reference","title":"SDK Reference","text":"<ul> <li>C# API Reference</li> <li>Python API Reference</li> <li>TypeScript API Reference</li> </ul>"},{"location":"security/","title":"Security","text":""},{"location":"security/#authentication","title":"Authentication","text":"<p>The WebSocketManager gRPC and HTTP endpoints accept connections authenticated via Dapr's service-to-service mTLS. All inter-service calls (API Gateway \u2192 WebSocketManager) are secured by Dapr's built-in mutual TLS.</p> <p>For external WebSocket servers, authentication is handled at the connection level:</p> <ul> <li>Managed connections: Credentials can be embedded in the WebSocket URL or exchanged via the connection's message protocol</li> <li>Binary pipeline: Messages are sent as raw bytes over WebSocket Binary or Text frames, determined by the <code>content_type</code> field. No correlation envelope wrapping occurs on external servers \u2014 correlation is handled via a 16-byte UUID prefix.</li> </ul> <p>For external client access to the WebSocketManager itself, authentication should be handled at the API Gateway layer. See API Gateway Security for JWT bearer token and role-based authorization configuration.</p>"},{"location":"security/#connection-ownership","title":"Connection Ownership","text":"<p>Each connection is owned by the service instance (Kubernetes pod) that created it. Only the owning instance can modify or disconnect a connection. The instance ID is derived from the <code>HOSTNAME</code> environment variable (Kubernetes pod name). If <code>HOSTNAME</code> is unset (e.g. local <code>dotnet run</code>), the instance ID falls back to a fresh GUID per process start.</p> <p>Per-connection routing uses heartbeat-based peer discovery and direct gRPC forwarding. Each instance periodically publishes its identity to the Dapr state store. When a connection-specific RPC (Disconnect, StartPublish, StopPublish, Send, SendRaw) lands on the wrong instance, the handler reads the connection's owner from the state store and forwards the call via a direct gRPC channel. On forward failure (peer dead), the forwarder reclaims the resource by re-stamping the state-store record with its own instance ID and retries locally.</p>"},{"location":"security/#pod-replacement","title":"Pod replacement","text":"<p>Kubernetes pod replacement (rolling update, eviction, node drain, HPA scale-down) changes the underlying compute. When a pod dies:</p> <ol> <li>The heartbeat service on surviving instances detects the missing peer    (heartbeat TTL expires) and removes it from the peer index.</li> <li>The next RPC targeting the dead pod's resources triggers a forward    attempt \u2192 Unavailable \u2192 reclamation (state-store record re-stamped,    resource re-created locally).</li> <li>For connections, the surviving instance creates a fresh    <code>ClientWebSocket</code> if <code>auto_reconnect=true</code>.</li> </ol> <p>Straggler connection records can be cleared on startup with <code>CLEAR_ALL_ON_START=TRUE</code>.</p>"},{"location":"security/#configuration-secrets","title":"Configuration &amp; Secrets","text":"<p>The WebSocketManager reads its configuration from the standard .NET configuration sources (in order: <code>appsettings.json</code>, environment variables, command-line). The following keys control runtime behavior and are the only ones that carry security-sensitive values:</p> Key Default Security role <code>Cors:AllowedOrigins</code> <code>[]</code> Restricts which browser origins can call the REST endpoints. Always set explicitly in production. <code>Logging:LogLevel:Default</code> <code>Information</code> Set to <code>Warning</code> or higher in production to avoid leaking sensitive request data into logs. <p>Connection Dapr component values (Redis password, broker credentials) are mounted at runtime via Dapr's secret management component (<code>secretstores/kubernetes</code>) \u2014 never commit them to source control. Dapr resolves them by key (e.g., <code>stateStore.password</code>) and the application reads them via <code>DaprClient.GetSecretAsync</code>.</p>"},{"location":"security/#dapr-component-security","title":"Dapr Component Security","text":"<p>The WebSocketManager relies on Dapr for state storage and pub/sub messaging. Ensure Dapr components are configured with:</p> <ul> <li>State store: Authentication enabled (Redis password for Valkey)</li> <li>Pub/sub: Authentication enabled on the message broker</li> <li>mTLS: Enabled in Dapr configuration (default since Dapr 1.0)</li> </ul>"},{"location":"security/#audit-logging","title":"Audit &amp; Logging","text":"<p>The WebSocketManager emits structured logs for every state-mutating operation (<code>connect</code>, <code>disconnect</code>, <code>send</code>, <code>start_publish</code>, <code>stop_publish</code>) and every reclaim sweep. Each log record includes the instance ID, connection ID, and a correlation ID derived from the incoming gRPC request metadata.</p> <p>Fire-and-forget logging calls (subscription health sweep, reclaim sweep) are wrapped in try/catch and downgrade to <code>LogLevel.Warning</code> if the underlying stream write fails \u2014 this prevents observability failures from cascading into service failures.</p>"},{"location":"security/#reporting-issues","title":"Reporting Issues","text":"<p>Report security concerns to the Virtufin security team.</p>"},{"location":"services/","title":"Services","text":"<p>This document provides detailed documentation for all service classes in the WebSocket Manager.</p>"},{"location":"services/#iwebsocketservice-websocketservice","title":"IWebSocketService / WebSocketService","text":"<p>Namespace: <code>Virtufin.WebSocketManager.Services</code></p> <p>Purpose: Core service for managing WebSocket connections. Provides high-level operations for connecting, disconnecting, listing, and sending messages through WebSocket connections.</p>"},{"location":"services/#interface-definition","title":"Interface Definition","text":"<pre><code>public interface IWebSocketService\n{\n    Task&lt;(WebSocketConnection? Connection, string? Error)&gt; ConnectAsync(\n        string url, bool autoReconnect, CancellationToken cancellationToken = default);\n\n    Task&lt;IEnumerable&lt;WebSocketConnection&gt;&gt; ListConnectionsAsync(\n        CancellationToken cancellationToken = default);\n\n    Task&lt;(bool Success, string? Error)&gt; DisconnectAsync(\n        string id, CancellationToken cancellationToken = default);\n\n    Task&lt;(bool Success, string? Error)&gt; StartPublishAsync(\n        string id, string topic, CancellationToken cancellationToken = default);\n\n    Task&lt;(bool Success, string? Error)&gt; StopPublishAsync(\n        string id, CancellationToken cancellationToken = default);\n\n    Task&lt;(byte[]? Response, string? Error)&gt; SendAsync(\n        string id, byte[] message, string contentType, int timeoutMs, CancellationToken cancellationToken = default);\n\n    Task&lt;(bool Success, string? Error)&gt; SendRawAsync(\n        string id, byte[] message, string contentType, CancellationToken cancellationToken = default);\n}\n</code></pre>"},{"location":"services/#connectasync","title":"ConnectAsync","text":"<p>Establishes a new WebSocket connection.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>url</code> | <code>string</code> | The WebSocket server URL (must be non-empty) | | <code>autoReconnect</code> | <code>bool</code> | Whether to enable auto-reconnect on failure | | <code>cancellationToken</code> | <code>CancellationToken</code> | Optional cancellation token |</p> <p>Returns: A tuple containing the created <code>WebSocketConnection</code> if successful, or <code>null</code> with an error message if unsuccessful.</p> <p>Behavior: 1. Validates that <code>url</code> is non-empty 2. Creates a new connection entry via <code>IWebSocketConnectionStore</code> 3. Connects via <code>IWebSocketClientWrapper</code> 4. Starts the receive loop to handle incoming messages 5. If publishing is configured, publishes messages to the topic via <code>IDaprPublisher</code> 6. Persists the connection to the store 7. On failure, removes the connection from the store and returns the error</p>"},{"location":"services/#listconnectionsasync","title":"ListConnectionsAsync","text":"<p>Retrieves all active WebSocket connections.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>cancellationToken</code> | <code>CancellationToken</code> | Optional cancellation token |</p> <p>Returns: An enumerable collection of all <code>WebSocketConnection</code> instances.</p>"},{"location":"services/#disconnectasync","title":"DisconnectAsync","text":"<p>Disconnects and removes a WebSocket connection.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>id</code> | <code>string</code> | The connection ID to disconnect | | <code>cancellationToken</code> | <code>CancellationToken</code> | Optional cancellation token |</p> <p>Returns: A tuple indicating success and an error message if unsuccessful.</p> <p>Behavior: 1. Retrieves the connection from the store 2. Validates ownership via <code>CheckOwnership</code> 3. Disconnects via <code>IWebSocketClientWrapper</code> (errors are logged but not thrown) 4. Removes the connection from the store</p>"},{"location":"services/#startpublishasync","title":"StartPublishAsync","text":"<p>Configures a connection to publish messages to a Dapr pub/sub topic.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>id</code> | <code>string</code> | The connection ID | | <code>topic</code> | <code>string</code> | The Dapr pub/sub topic name (must be non-empty) | | <code>cancellationToken</code> | <code>CancellationToken</code> | Optional cancellation token |</p> <p>Returns: A tuple indicating success and an error message if unsuccessful.</p> <p>Errors: - <code>\"Connection not found\"</code> - Connection ID does not exist - Ownership errors from <code>CheckOwnership</code> - <code>\"WebSocket is not connected\"</code> - Connection status is not Connected - <code>\"Topic is required\"</code> - Topic parameter is empty</p>"},{"location":"services/#stoppublishasync","title":"StopPublishAsync","text":"<p>Stops publishing messages for a connection.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>id</code> | <code>string</code> | The connection ID | | <code>cancellationToken</code> | <code>CancellationToken</code> | Optional cancellation token |</p> <p>Returns: A tuple indicating success and an error message if unsuccessful.</p> <p>Behavior: Sets <code>connection.Topic</code> to <code>null</code> and updates the store.</p>"},{"location":"services/#sendasync","title":"SendAsync","text":"<p>Sends a message and waits for a correlated response.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>id</code> | <code>string</code> | The connection ID | | <code>message</code> | <code>string</code> | The message to send | | <code>timeoutMs</code> | <code>int</code> | Timeout in milliseconds to wait for response | | <code>cancellationToken</code> | <code>CancellationToken</code> | Optional cancellation token |</p> <p>Returns: A tuple containing the response message if successful, or <code>null</code> with an error message.</p> <p>Errors: - <code>\"Connection not found\"</code> - Ownership errors - <code>\"WebSocket is not connected\"</code> - <code>\"Request timeout\"</code> - Timeout exceeded</p> <p>Implementation Detail: The message is wrapped with a correlation ID and sent via <code>IWebSocketClientWrapper.SendAndWaitAsync</code>. Responses are matched by correlation ID.</p> <p>Remote-server contract (required): <code>SendAsync</code> only returns a response if the remote server cooperates with the correlation protocol. The correlation envelope is a 16-byte UUID binary prefix applied uniformly to all frame types:</p> <ul> <li>The wrapper prepends a 16-byte correlation UUID to the outgoing message: <code>[16 bytes UUID][payload bytes]</code></li> <li>The remote server must prepend the same 16-byte UUID prefix to its response frame: <code>[16 bytes UUID][response bytes]</code></li> <li>The receive loop scans every incoming frame, matches the first 16 bytes against pending correlation UUIDs, strips the prefix, and delivers the remaining bytes to the caller</li> <li>Servers that don't echo the 16-byte UUID prefix will trigger a <code>\"Request timeout\"</code> after <code>timeoutMs</code> regardless of whether they actually sent a response</li> </ul> <p>This binary prefix protocol replaces the prior JSON <code>WrappedMessage</code> envelope (<code>{\"id\": \"...\", \"payload\": \"...\"}</code>) and is the canonical correlation contract for all content types (text, binary, JSON). See <code>WebSocketClientWrapper</code> below for the implementation details and <code>SendAndWaitAsync</code> for the async variant.</p>"},{"location":"services/#sendrawasync","title":"SendRawAsync","text":"<p>Sends a raw message without waiting for a response.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>id</code> | <code>string</code> | The connection ID | | <code>message</code> | <code>byte[]</code> | The raw bytes to send (UTF-8 encoded if text) | | <code>cancellationToken</code> | <code>CancellationToken</code> | Optional cancellation token |</p> <p>Returns: A tuple indicating success and an error message if unsuccessful.</p>"},{"location":"services/#checkownership-private","title":"CheckOwnership (Private)","text":"<p>Validates that the current instance owns the connection.</p> <pre><code>private bool CheckOwnership(WebSocketConnection connection, out string? error)\n{\n    error = null;\n    if (connection.InstanceId != _instanceIdProvider.GetInstanceId())\n    {\n        error = $\"Connection owned by different instance: {connection.InstanceId}\";\n        return false;\n    }\n    return true;\n}\n</code></pre> <p>Purpose: Prevents cross-instance modifications in distributed deployments.</p>"},{"location":"services/#iwebsocketconnectionstore-distributedwebsocketconnectionstore","title":"IWebSocketConnectionStore / DistributedWebSocketConnectionStore","text":"<p>Namespace: <code>Virtufin.WebSocketManager.Services</code></p> <p>Purpose: Manages WebSocket connection storage with distributed persistence via Dapr state store.</p>"},{"location":"services/#interface-definition_1","title":"Interface Definition","text":"<pre><code>public interface IWebSocketConnectionStore\n{\n    Task&lt;WebSocketConnection&gt; CreateConnectionAsync(\n        string url, bool autoReconnect, CancellationToken cancellationToken = default);\n\n    Task&lt;WebSocketConnection?&gt; GetConnectionAsync(\n        string id, CancellationToken cancellationToken = default);\n\n    Task&lt;IEnumerable&lt;WebSocketConnection&gt;&gt; GetAllConnectionsAsync(\n        CancellationToken cancellationToken = default);\n\n    Task UpdateConnectionAsync(\n        WebSocketConnection connection, CancellationToken cancellationToken = default);\n\n    Task RemoveConnectionAsync(\n        string id, CancellationToken cancellationToken = default);\n\n    Task ClearAllConnectionsAsync(\n        CancellationToken cancellationToken = default);\n}\n</code></pre>"},{"location":"services/#implementation-distributedwebsocketconnectionstore","title":"Implementation: DistributedWebSocketConnectionStore","text":"<p>Uses a two-tier storage approach: - Local cache: <code>ConcurrentDictionary&lt;string, WebSocketConnection&gt;</code> for fast in-memory access - Dapr state store: Via <code>IDaprConnectionRepository</code> for distributed persistence</p>"},{"location":"services/#createconnectionasync","title":"CreateConnectionAsync","text":"<p>Creates a new connection with a generated ID and the current instance ID.</p> <p>Returns: A new <code>WebSocketConnection</code> with: - <code>Id</code> = <code>Guid.NewGuid().ToString()</code> - <code>Url</code> = provided URL - <code>AutoReconnect</code> = provided value - <code>Status</code> = <code>ConnectionStatus.Disconnected</code> - <code>CreatedAt</code> = <code>DateTime.UtcNow</code> - <code>InstanceId</code> = from <code>IInstanceIdProvider.GetInstanceId()</code></p> <p>The connection is added to the local cache but NOT persisted until explicitly updated.</p>"},{"location":"services/#getconnectionasync","title":"GetConnectionAsync","text":"<p>Retrieves a connection by ID.</p> <p>Behavior: 1. First checks local cache (<code>_localConnections</code>) 2. If not found, retrieves from Dapr state store via <code>IDaprConnectionRepository</code></p> <p>Returns: The connection if found, otherwise <code>null</code>.</p>"},{"location":"services/#getallconnectionsasync","title":"GetAllConnectionsAsync","text":"<p>Retrieves all connections from both local cache and Dapr state store.</p> <p>Returns: Combined list of remote and local connections.</p> <p>Merge Logic: Remote connections (from Dapr state store) are returned first. Local connections not present in the remote set are then appended. If a connection exists in both stores (e.g., updated locally but not yet persisted), the remote version takes precedence.</p> <p>Deduplication key: <code>WebSocketConnection.Id</code> (the connection ID string assigned at <code>Connect</code> time). Two entries are considered the same connection iff <code>local.Id == remote.Id</code>. The merge never produces duplicates for the same <code>Id</code>; when both sides have an entry for the same <code>Id</code>, the remote entry wins. Note: the local cache always reflects the most recent in-memory state, but the remote store is the source of truth for distributed queries \u2014 so a connection updated locally but not yet persisted may appear with stale data in the remote entry until the next <code>UpdateConnectionAsync</code> call.</p>"},{"location":"services/#updateconnectionasync","title":"UpdateConnectionAsync","text":"<p>Updates both local cache and Dapr state store.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>connection</code> | <code>WebSocketConnection</code> | The connection to update |</p> <p>Behavior: 1. Updates local cache 2. Converts to <code>DaprStateStoreEntry</code> and saves via repository</p>"},{"location":"services/#removeconnectionasync","title":"RemoveConnectionAsync","text":"<p>Removes a connection from both local cache and Dapr state store.</p>"},{"location":"services/#clearallconnectionsasync","title":"ClearAllConnectionsAsync","text":"<p>Removes all connections from both storage tiers.</p>"},{"location":"services/#idaprconnectionrepository-daprconnectionrepository","title":"IDaprConnectionRepository / DaprConnectionRepository","text":"<p>Namespace: <code>Virtufin.WebSocketManager.Services</code></p> <p>Purpose: Handles Dapr state store operations for WebSocket connections.</p>"},{"location":"services/#interface-definition_2","title":"Interface Definition","text":"<pre><code>public interface IDaprConnectionRepository\n{\n    Task&lt;IEnumerable&lt;DaprStateStoreEntry&gt;&gt; GetAllAsync(\n        CancellationToken cancellationToken = default);\n\n    Task&lt;DaprStateStoreEntry?&gt; GetAsync(\n        string id, CancellationToken cancellationToken = default);\n\n    Task SaveAsync(\n        DaprStateStoreEntry entry, CancellationToken cancellationToken = default);\n\n    Task DeleteAsync(\n        string id, CancellationToken cancellationToken = default);\n}\n</code></pre>"},{"location":"services/#implementation-daprconnectionrepository","title":"Implementation: DaprConnectionRepository","text":"<p>Uses Dapr state store with an index pattern for efficient lookups.</p>"},{"location":"services/#state-store-structure","title":"State Store Structure","text":"<ul> <li>Connection entries: Key = <code>websocket-{id}</code>, Value = <code>DaprStateStoreEntry</code></li> <li>Index: Key = <code>websocket-index</code>, Value = <code>HashSet&lt;string&gt;</code> containing all connection IDs</li> </ul>"},{"location":"services/#getallasync","title":"GetAllAsync","text":"<p>Retrieves all connections via the index: 1. Gets the index (<code>HashSet&lt;string&gt;</code>) from state store 2. If index is null or empty, returns empty enumeration 3. Constructs bulk state keys and retrieves all entries in one call 4. Returns only non-null entries</p>"},{"location":"services/#getasync","title":"GetAsync","text":"<p>Retrieves a single connection by ID.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>id</code> | <code>string</code> | The connection ID |</p> <p>Returns: <code>DaprStateStoreEntry</code> if found, otherwise <code>null</code>.</p>"},{"location":"services/#saveasync","title":"SaveAsync","text":"<p>Saves a connection entry and updates the index.</p> <p>Behavior: 1. Saves the entry with key <code>websocket-{entry.Id}</code> 2. Calls <code>UpdateIndexAsync</code> with <code>add: true</code></p>"},{"location":"services/#deleteasync","title":"DeleteAsync","text":"<p>Deletes a connection entry and updates the index.</p> <p>Behavior: 1. Deletes the entry with key <code>websocket-{id}</code> 2. Calls <code>UpdateIndexAsync</code> with <code>add: false</code></p>"},{"location":"services/#updateindexasync-private","title":"UpdateIndexAsync (Private)","text":"<p>Updates the connection index in state store.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>id</code> | <code>string</code> | The connection ID | | <code>add</code> | <code>bool</code> | <code>true</code> to add to index, <code>false</code> to remove |</p> <p>Behavior: 1. Gets current index (or creates new <code>HashSet&lt;string&gt;</code> if null) 2. Adds or removes the ID 3. Saves the updated index</p>"},{"location":"services/#iwebsocketclientwrapper-websocketclientwrapper","title":"IWebSocketClientWrapper / WebSocketClientWrapper","text":"<p>Namespace: <code>Virtufin.WebSocketManager.Services</code></p> <p>Purpose: Provides low-level WebSocket connection operations. Manages external WebSocket connections including connection lifecycle, message sending/receiving, and auto-reconnect logic.</p>"},{"location":"services/#interface-definition_3","title":"Interface Definition","text":"<pre><code>public interface IWebSocketClientWrapper\n{\n    Task ConnectAsync(WebSocketConnection connection);\n\n    Task DisconnectAsync(WebSocketConnection connection);\n\n    Task SendAsync(WebSocketConnection connection, byte[] message, string contentType);\n\n    Task&lt;byte[]&gt; SendAndWaitAsync(\n        WebSocketConnection connection, byte[] message, int timeoutMs, string contentType);\n\n    void StartReceiveLoop(\n        WebSocketConnection connection, Func&lt;byte[], string, Task&gt; onMessageReceived);\n}\n</code></pre>"},{"location":"services/#implementation-websocketclientwrapper","title":"Implementation: WebSocketClientWrapper","text":"<p>Manages <code>System.Net.WebSockets.ClientWebSocket</code> instances with a 16-byte UUID binary prefix for request-response correlation.</p>"},{"location":"services/#internal-state","title":"Internal State","text":"<ul> <li><code>_pendingRequests</code>: <code>ConcurrentDictionary&lt;Guid, TaskCompletionSource&lt;byte[]&gt;&gt;</code> - Maps correlation UUIDs to pending response completers</li> <li>Correlation envelope: <code>[16 bytes UUID][payload bytes]</code> \u2014 No JSON parsing, fast binary matching</li> </ul>"},{"location":"services/#connectasync_1","title":"ConnectAsync","text":"<p>Establishes a WebSocket connection.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>connection</code> | <code>WebSocketConnection</code> | The connection to connect |</p> <p>Behavior: 1. Creates a new <code>CancellationTokenSource</code> and assigns to <code>connection.CancellationTokenSource</code> 2. Creates a new <code>ClientWebSocket</code> and assigns to <code>connection.WebSocket</code> 3. Connects to <code>connection.Url</code> 4. Sets <code>connection.Status</code> to <code>Connected</code> 5. On failure, sets status to <code>Disconnected</code> and re-throws</p> <p>Throws: Exception if connection fails.</p>"},{"location":"services/#disconnectasync_1","title":"DisconnectAsync","text":"<p>Gracefully closes a WebSocket connection.</p> <p>Behavior: 1. Cancels the <code>CancellationTokenSource</code> 2. If socket is open, sends close frame with <code>NormalClosure</code> 3. Disposes the WebSocket 4. Sets status to <code>Disconnected</code></p>"},{"location":"services/#sendasync_1","title":"SendAsync","text":"<p>Sends bytes via WebSocket. Frame type (Text/Binary) is determined by the <code>contentType</code> parameter: - <code>text/*</code> or <code>application/json</code> \u2192 Text frame - Everything else \u2192 Binary frame</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>connection</code> | <code>WebSocketConnection</code> | The connection to send on | | <code>message</code> | <code>byte[]</code> | The raw bytes to send | | <code>contentType</code> | <code>string</code> | MIME content type (e.g., \"text/plain\", \"application/json\", \"application/octet-stream\") |</p> <p>Throws: <code>InvalidOperationException</code> if WebSocket is not connected.</p>"},{"location":"services/#sendandwaitasync","title":"SendAndWaitAsync","text":"<p>Sends bytes with a 16-byte UUID binary prefix for correlation, waits for a matching response (first 16 bytes match the UUID prefix, stripped from the response).</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>connection</code> | <code>WebSocketConnection</code> | The connection to send on | | <code>message</code> | <code>byte[]</code> | The message to send (min 16 bytes) | | <code>timeoutMs</code> | <code>int</code> | Timeout in milliseconds | | <code>contentType</code> | <code>string</code> | MIME content type |</p> <p>Behavior: 1. Generates a new correlation UUID 2. Prepends 16-byte UUID to the message: <code>[16 bytes UUID][payload bytes]</code> 3. Sends the wrapped bytes via <code>SendAsync</code> with the given contentType 4. Waits for a response whose first 16 bytes match the UUID 5. Returns the response with the 16-byte UUID prefix stripped</p> <p>Implementation Detail: The message is wrapped with a 16-byte GUID prefix. Responses are matched in <code>StartReceiveLoop</code> by comparing the first 16 bytes of received messages against pending correlation UUIDs.</p>"},{"location":"services/#startreceiveloop","title":"StartReceiveLoop","text":"<p>Starts a background task to receive messages.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>connection</code> | <code>WebSocketConnection</code> | The connection to receive from | | <code>onMessageReceived</code> | <code>Func&lt;byte[], string, Task&gt;</code> | Callback for received messages (raw bytes + auto-detected ContentType) |</p> <p>Behavior: 1. Cancels any existing receive operation 2. Starts a new <code>Task</code> running the receive loop 3. Assigns to <code>connection.ReceiveTask</code></p> <p>Receive Loop: - Uses 8192-byte buffer - Accumulates messages until <code>EndOfMessage</code> is true - Matches the first 16 bytes of received messages against pending correlation UUIDs - If correlation ID matches a pending request, completes that request - Otherwise, invokes <code>onMessageReceived</code> callback</p> <p>Error Handling: - <code>OperationCanceledException</code>: Silently exits (expected on disconnect) - Other exceptions: If <code>connection.AutoReconnect</code> is true, attempts reconnection; otherwise marks as <code>Disconnected</code></p>"},{"location":"services/#tryreconnectasync-private","title":"TryReconnectAsync (Private)","text":"<p>Attempts to reconnect with exponential backoff.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>connection</code> | <code>WebSocketConnection</code> | The connection to reconnect | | <code>onMessageReceived</code> | <code>Func&lt;string, Task&gt;</code> | Callback for received messages |</p> <p>Behavior: 1. Retries up to <code>WebSocketOptions.ReconnectMaxAttempts</code> times 2. Delay doubles each attempt: <code>baseDelay * 2^(attempt-1)</code>, capped at 30 seconds 3. On successful reconnect, restarts the receive loop 4. On all failures, sets status to <code>Disconnected</code></p>"},{"location":"services/#idaprpublisher-daprpublisher","title":"IDaprPublisher / DaprPublisher","text":"<p>Namespace: <code>Virtufin.WebSocketManager.Services</code></p> <p>Purpose: Publishes WebSocket messages to Dapr pub/sub topics.</p>"},{"location":"services/#interface-definition_4","title":"Interface Definition","text":"<pre><code>public interface IDaprPublisher\n{\n    Task PublishAsync(\n        string topic, string websocketId, string websocketUrl, byte[] payload, string? contentType = null);\n}\n</code></pre>"},{"location":"services/#implementation-daprpublisher","title":"Implementation: DaprPublisher","text":""},{"location":"services/#publishasync","title":"PublishAsync","text":"<p>Publishes a WebSocket message directly to a Dapr pub/sub topic. Metadata (websocketId, websocketUrl, timestamp, contentType) is sent as CloudEvent extensions rather than wrapped inside the data payload.</p> <p>Parameters: | Parameter | Type | Description | |-----------|------|-------------| | <code>topic</code> | <code>string</code> | The topic name to publish to | | <code>websocketId</code> | <code>string</code> | The ID of the WebSocket connection | | <code>websocketUrl</code> | <code>string</code> | The URL of the WebSocket connection | | <code>payload</code> | <code>byte[]</code> | The raw WebSocket message bytes | | <code>contentType</code> | <code>string?</code> | Optional content type from the WebSocket frame |</p> <p>Behavior: 1. Decodes the payload bytes as UTF-8 text 2. Passes the text directly to <code>DaprClient.PublishEventAsync&lt;string&gt;</code> with the    configured pub/sub component 3. Metadata fields (<code>websocketId</code>, <code>websocketUrl</code>, <code>timestamp</code>, <code>contentType</code>)    are attached as CloudEvent extensions (passed via Dapr metadata dictionary)    rather than wrapped inside the data payload</p> <p>Dapr CloudEvent format (as stored in Redis): <pre><code>{\n  \"data\": \"{\\\"stream\\\":\\\"btcusdt@depth@100ms\\\",\\\"data\\\":{\\\"e\\\":\\\"depthUpdate\\\",...}}\",\n  \"datacontenttype\": \"application/json\",\n  \"websocketId\": \"352eb904-...\",\n  \"websocketUrl\": \"wss://stream.binance.com:9443/stream\",\n  \"timestamp\": \"2026-06-12T18:48:16.939543Z\",\n  \"contentType\": \"text/plain\",\n  \"type\": \"com.dapr.event.sent\"\n}\n</code></pre> The <code>data</code> field contains the raw WebSocket message as a JSON string (no base64 encoding). Consumers read <code>data</code> directly and JSON-parse it.</p> <p>Throws: Exception if publishing fails.</p>"},{"location":"services/#iinstanceidprovider-defaultinstanceidprovider","title":"IInstanceIdProvider / DefaultInstanceIdProvider","text":"<p>Namespace: <code>Virtufin.WebSocketManager.Services</code></p> <p>Purpose: Provides a unique identifier for the current service instance.</p>"},{"location":"services/#interface-definition_5","title":"Interface Definition","text":"<pre><code>public interface IInstanceIdProvider\n{\n    string GetInstanceId();\n}\n</code></pre>"},{"location":"services/#implementation-defaultinstanceidprovider","title":"Implementation: DefaultInstanceIdProvider","text":"<p>Returns the pod name from the <code>HOSTNAME</code> environment variable (set by Kubernetes). Falls back to a random GUID if <code>HOSTNAME</code> is not set.</p> <p>Connection Ownership: Connections are tied to the instance that created them. Only the owning instance can disconnect or send messages on a connection. Pod replacement is handled via heartbeat-based peer discovery and lazy reclamation: orphaned connections are re-stamped on the next forwarded RPC. Set <code>CLEAR_ALL_ON_START=TRUE</code> to clear all connections from the state store on startup instead of reclaiming reconnected state.</p> <p>Use Case: Used by <code>WebSocketService</code> to validate connection ownership and prevent cross-instance modifications.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Common failure modes and recovery procedures for the Virtufin WebSocketManager.</p> <p>See also: Development guide \u2192 Troubleshooting for service-startup, port-conflict, Dapr sidecar, and protoc-generation issues. Deployment guide \u2192 Troubleshooting for Kubernetes pod, image-pull, and readiness/liveness probe issues.</p>"},{"location":"troubleshooting/#operation-level-failures","title":"Operation-level failures","text":""},{"location":"troubleshooting/#connection-not-found-when-sending-a-message","title":"\"Connection not found\" when sending a message","text":"<p>Symptom: <code>Send</code>, <code>SendRaw</code>, <code>StartPublish</code>, or <code>StopPublish</code> returns <code>success=false, error=\"Connection not found\"</code>.</p> <p>Likely causes: - The connection ID is wrong (typo, stale state from a previous instance) - The connection was reclaimed by another instance after the original owner died   because the owning instance died - The connection was explicitly disconnected and removed from the state store</p> <p>Diagnose:</p> <pre><code># List all active connections\ncurl http://localhost:5001/connections\n\n# Check the reclaim sweeper logs\nkubectl logs -n virtufin -l app.kubernetes.io/name=virtufin-websocketmanager | grep -i \"reclaim\"\n\n# Check if the owning instance is still running\nkubectl get pods -n virtufin -l app.kubernetes.io/name=virtufin-websocketmanager\n</code></pre>"},{"location":"troubleshooting/#connection-owned-by-different-instance-error","title":"\"Connection owned by different instance\" error","text":"<p>Symptom: Operations return <code>error=\"Connection owned by different instance: &lt;instance-id&gt;\"</code>.</p> <p>Likely cause: The current pod's <code>HOSTNAME</code> does not match the connection's <code>InstanceId</code>. This happens after a rolling update where the new pod receives a new <code>HOSTNAME</code> before the old pod's connections have been reclaimed.</p> <p>Fix: Wait for the heartbeat service to detect the stale peer (up to 60s by default) and the next RPC to trigger lazy reclamation. This may reclaim the connection. The new pod can then issue a <code>Connect</code> to recreate the connection under the new <code>InstanceId</code>.</p>"},{"location":"troubleshooting/#websocket-is-not-connected-error","title":"\"WebSocket is not connected\" error","text":"<p>Symptom: <code>Send</code> or <code>SendRaw</code> returns <code>error=\"WebSocket is not connected\"</code>.</p> <p>Likely cause: The underlying <code>ClientWebSocket</code> has been closed or the remote endpoint has dropped the connection.</p> <p>Fix: 1. If <code>autoReconnect=true</code>, the wrapper will attempt reconnection with    exponential backoff (up to <code>ReconnectMaxAttempts</code> retries) 2. If <code>autoReconnect=false</code>, the connection must be re-<code>Connect</code>-ed manually</p>"},{"location":"troubleshooting/#request-timeout-from-sendasync","title":"\"Request timeout\" from SendAsync","text":"<p>Symptom: <code>SendAsync</code> returns <code>error=\"Request timeout\"</code> after the configured timeout.</p> <p>Likely cause: The remote WebSocket server did not send a response with a matching correlation UUID (16-byte prefix) within the timeout window.</p> <p>Diagnose:</p> <pre><code># Check the receive-loop logs\nkubectl logs -n virtufin -l app.kubernetes.io/name=virtufin-websocketmanager | grep -i \"correlation\"\n\n# Increase the timeout in the client\nawait client.send(connection_id, message, timeoutMs=60000)  # 60s instead of default\n</code></pre>"},{"location":"troubleshooting/#connection-lifecycle-failures","title":"Connection lifecycle failures","text":""},{"location":"troubleshooting/#pod-restarts-and-orphaned-connections","title":"Pod restarts and orphaned connections","text":"<p>Symptom: After a pod restart, the new pod reports connections that don't match its <code>InstanceId</code>.</p> <p>Expected behavior: Heartbeat-based peer discovery (lazy rebalance interval) detects dead instances and reassigns their connections. The new pod then sees the reclaimed connections with its own <code>InstanceId</code>.</p> <p>Force a clean slate:</p> <pre><code># Set CLEAR_ALL_ON_START=TRUE in the deployment env\n# This clears ALL connections from the state store on startup\n# instead of reclaiming reconnected state\n\nkubectl set env deployment/virtufin-websocketmanager CLEAR_ALL_ON_START=TRUE\nkubectl rollout restart deployment/virtufin-websocketmanager\n</code></pre> <p>Warning: <code>CLEAR_ALL_ON_START=TRUE</code> is destructive \u2014 it disconnects all active WebSocket clients. Use only during planned maintenance or recovery from a known-bad state.</p>"},{"location":"troubleshooting/#state-store-is-out-of-sync","title":"State store is out of sync","text":"<p>Symptom: The local cache shows connections that don't appear in the distributed state store, or vice versa.</p> <p>Diagnose:</p> <pre><code># Inspect the state store directly\ndocker exec -it dapr_redis_1 redis-cli KEYS 'websocket-*'\ndocker exec -it dapr_redis_1 redis-cli SMEMBERS websocket-index\n</code></pre> <p>Fix: Restart all WebSocketManager replicas to force a fresh <code>IWebSocketConnectionStore.GetAllConnectionsAsync</code> call and rebuild the local cache.</p>"},{"location":"troubleshooting/#pubsub-failures","title":"Pub/Sub failures","text":""},{"location":"troubleshooting/#published-messages-not-reaching-subscribers","title":"Published messages not reaching subscribers","text":"<p>Symptom: <code>StartPublish</code> returns success but downstream subscribers never receive the messages.</p> <p>Likely cause: The Dapr pub/sub component (Redis/Valkey pubsub) is not configured, or the topic name in <code>StartPublish</code> doesn't match the subscriber's expected topic.</p> <p>Diagnose:</p> <pre><code># Check the publisher logs\nkubectl logs -n virtufin -l app.kubernetes.io/name=virtufin-websocketmanager | grep -i \"publish\\|topic\"\n\n# Check the Dapr pub/sub component\nkubectl get components -n virtufin pubsub -o yaml\n\n# Verify the topic exists in Redis\ndocker exec -it dapr_redis_1 redis-cli PUBSUB CHANNELS '*'\n</code></pre>"},{"location":"troubleshooting/#recovery-procedures","title":"Recovery procedures","text":""},{"location":"troubleshooting/#restart-the-websocketmanager","title":"Restart the WebSocketManager","text":"<pre><code>kubectl rollout restart deployment/virtufin-websocketmanager -n virtufin\nkubectl rollout status deployment/virtufin-websocketmanager -n virtufin\n</code></pre> <p>A rolling restart is non-disruptive \u2014 heartbeat-based peer forwarding will move connections to surviving replicas.</p>"},{"location":"troubleshooting/#wipe-all-connections","title":"Wipe all connections","text":"<pre><code># Manual: clear the state store directly\ndocker exec -it dapr_redis_1 redis-cli DEL websocket-index\ndocker exec -it dapr_redis_1 redis-cli --scan --pattern 'websocket-*' | xargs -L 100 docker exec -i dapr_redis_1 redis-cli DEL\n</code></pre> <p>Note: This is destructive and will disconnect all active WebSocket clients. Use only for recovery from a corrupted state.</p>"},{"location":"troubleshooting/#reporting-issues","title":"Reporting issues","text":"<p>If none of the above resolves your issue, gather the following and file a Gitea issue:</p> <ul> <li>WebSocketManager version (<code>LIBRARY_VERSION</code> from the build info endpoint)</li> <li>Connection ID(s) affected</li> <li>Owning instance ID (from the connection's <code>InstanceId</code> field)</li> <li>Full error message and gRPC status code</li> <li>Relevant log lines from the WebSocketManager and any affected backend</li> </ul>"},{"location":"typescript-client/","title":"TypeScript Client Library","text":"<p>The Virtufin WebSocketManager TypeScript client library (<code>@virtufin/websocketmanager</code>) provides a TypeScript interface for managing external WebSocket connections using gRPC-web and Connect-RPC.</p>"},{"location":"typescript-client/#installation","title":"Installation","text":""},{"location":"typescript-client/#from-gitea-npm-registry","title":"From Gitea npm Registry","text":"<p>Map the <code>@virtufin</code> scope to the registry and add a token (scope: <code>read:package</code>):</p> <pre><code>cat &gt;&gt; ~/.npmrc &lt;&lt;'EOF'\n@virtufin:registry=https://gitea.haenerconsulting.com/api/packages/virtufin/npm/\n//gitea.haenerconsulting.com/api/packages/virtufin/npm/:_authToken=&lt;your-gitea-token&gt;\nEOF\n</code></pre> <p>Use <code>gitea.haenerconsulting.com</code>, not the <code>npm.*</code> alias: Gitea advertises <code>dist.tarball</code> on <code>gitea.*</code> whatever host you query, so the <code>npm.*</code> form fetches metadata authenticated and then follows an unauthenticated redirect for the payload \u2014 npm matches tokens by URL prefix.</p> <p>Then install:</p> <pre><code>npm install @virtufin/websocketmanager\n</code></pre>"},{"location":"typescript-client/#from-local-source","title":"From Local Source","text":"<pre><code>{\n  \"dependencies\": {\n    \"@virtufin/websocketmanager\": \"file:path/to/src/typescript\"\n  }\n}\n</code></pre>"},{"location":"typescript-client/#overview","title":"Overview","text":"<p>The client library provides the <code>WebSocketManagerClient</code> class as the main entry point for WebSocket connection management.</p>"},{"location":"typescript-client/#quick-start","title":"Quick Start","text":"<pre><code>import { WebSocketManagerClient } from \"@virtufin/websocketmanager\";\n\nconst client = new WebSocketManagerClient({ url: \"http://localhost:5001\" });\n\n// Connect to a WebSocket server\nconst { id: connectionId, status } = await client.connect({\n    url: \"wss://echo.websocket.org\",\n    autoReconnect: false\n});\nconsole.log(`Connected: ${connectionId} - ${status}`);\n\n// List all connections\nconst { connections } = await client.list({});\nconsole.log(`Total connections: ${connections.length}`);\n\n// Send a message\nconst { response } = await client.send({\n    id: connectionId,\n    message: JSON.stringify({ type: \"ping\" }),\n    timeoutMs: 5000\n});\nconsole.log(`Response: ${response}`);\n\n// Disconnect\nawait client.disconnect({ id: connectionId });\n</code></pre>"},{"location":"typescript-client/#websocketmanagerclient","title":"WebSocketManagerClient","text":""},{"location":"typescript-client/#constructor","title":"Constructor","text":"<pre><code>const client = new WebSocketManagerClient({ url: string, timeout?: number });\n</code></pre> <p>Parameters: - <code>url</code> - The gRPC-web base URL (e.g., <code>\"http://localhost:5001\"</code>) - <code>timeout</code> - Request timeout in milliseconds (default: <code>30000</code>)</p>"},{"location":"typescript-client/#methods","title":"Methods","text":""},{"location":"typescript-client/#connection-management","title":"Connection Management","text":"<pre><code>// Connect to a WebSocket server\nconnect(request: ConnectRequest): Promise&lt;ConnectResponse&gt;\n\n// Disconnect from a WebSocket server\ndisconnect(request: DisconnectRequest): Promise&lt;DisconnectResponse&gt;\n\n// List all managed connections\nlist(request: ListRequest = {}): Promise&lt;ListResponse&gt;\n</code></pre> <p>Example: <pre><code>const { id: connectionId, status } = await client.connect({\n    url: \"wss://echo.websocket.org\",\n    autoReconnect: true\n});\n\nconst { connections } = await client.list({});\nfor (const conn of connections) {\n    console.log(`${conn.id}: ${conn.url} [${conn.status}]`);\n}\n\nawait client.disconnect({ id: connectionId });\n</code></pre></p>"},{"location":"typescript-client/#pubsub-integration","title":"Pub/Sub Integration","text":"<pre><code>// Start publishing WebSocket messages to a Dapr pub/sub topic\nstartPublish(request: StartPublishRequest): Promise&lt;StartPublishResponse&gt;\n\n// Stop publishing to the topic\nstopPublish(request: StopPublishRequest): Promise&lt;StopPublishResponse&gt;\n</code></pre> <p>Example: <pre><code>await client.startPublish({\n    id: connectionId,\n    topic: \"websocket-events\"\n});\n\nawait client.stopPublish({ id: connectionId });\n</code></pre></p>"},{"location":"typescript-client/#messaging","title":"Messaging","text":"<pre><code>// Send a message and wait for a correlated response\nsend(request: SendRequest): Promise&lt;SendResponse&gt;\n\n// Send a message without waiting for a response\nsendRaw(request: SendRawRequest): Promise&lt;SendRawResponse&gt;\n</code></pre> <p>Example: <pre><code>// Send with correlation (expects response)\nconst { response } = await client.send({\n    id: connectionId,\n    message: JSON.stringify({ type: \"request\", id: \"123\" }),\n    timeoutMs: 5000\n});\nconsole.log(`Response: ${response}`);\n\n// Fire-and-forget\nawait client.sendRaw({\n    id: connectionId,\n    message: JSON.stringify({ type: \"notification\" })\n});\n</code></pre></p>"},{"location":"typescript-client/#tags","title":"Tags","text":"<pre><code>// Set/replace a single tag on a connection (resource metadata \u2014 not forwarded to remote WS)\nsetTag(request: SetTagRequest): Promise&lt;SetTagResponse&gt;\n\n// Get a single tag value\ngetTag(request: GetTagRequest): Promise&lt;GetTagResponse&gt;\n\n// Replace all tags on a connection at once\nsetTags(request: SetTagsRequest): Promise&lt;SetTagsResponse&gt;\n\n// Get all tags for a connection\ngetTags(request: GetTagsRequest): Promise&lt;GetTagsResponse&gt;\n</code></pre> <p>Example: <pre><code>// Set a single tag\nawait client.setTag({ id: connectionId, key: \"environment\", value: \"staging\" });\n\n// Replace all tags\nawait client.setTags({\n    id: connectionId,\n    tags: { environment: \"staging\", team: \"payments\" }\n});\n\n// Read back\nconst { tags } = await client.getTags({ id: connectionId });\nconsole.log(`Tags:`, tags);\n\n// Filter connections by tag via ListRequest.Filters\nconst listResult = await client.list({\n    filters: [{ key: \"environment\", value: \"staging\" }]\n});\n</code></pre></p>"},{"location":"typescript-client/#data-models","title":"Data Models","text":""},{"location":"typescript-client/#connectrequest","title":"ConnectRequest","text":"<pre><code>interface ConnectRequest {\n    url: string;\n    autoReconnect?: boolean;\n}\n</code></pre>"},{"location":"typescript-client/#connectresponse","title":"ConnectResponse","text":"<pre><code>interface ConnectResponse {\n    id: string;\n    status: string;\n}\n</code></pre>"},{"location":"typescript-client/#websocketconnection","title":"WebSocketConnection","text":"<pre><code>interface WebSocketConnection {\n    id: string;\n    url: string;\n    status: string;\n    topic: string;\n    instanceId: string;\n}\n</code></pre>"},{"location":"typescript-client/#sendrequest","title":"SendRequest","text":"<pre><code>interface SendRequest {\n    id: string;\n    message: string;\n    timeoutMs: number;\n}\n</code></pre>"},{"location":"typescript-client/#sendresponse","title":"SendResponse","text":"<pre><code>interface SendResponse {\n    response: string;\n}\n</code></pre>"},{"location":"typescript-client/#error-handling","title":"Error Handling","text":""},{"location":"typescript-client/#connection-not-found","title":"Connection Not Found","text":"<pre><code>try {\n    await client.disconnect({ id: \"nonexistent\" });\n} catch (error) {\n    if (error instanceof ConnectError) {\n        console.log(`Not found: ${error.message}`);\n    }\n}\n</code></pre>"},{"location":"typescript-client/#send-timeout","title":"Send Timeout","text":"<pre><code>try {\n    const { response } = await client.send({\n        id: connectionId,\n        message: \"slow request\",\n        timeoutMs: 100 // very short\n    });\n} catch (error) {\n    if (error instanceof ConnectError &amp;&amp; error.code === Code.DeadlineExceeded) {\n        console.log(\"Send timed out\");\n    }\n}\n</code></pre>"},{"location":"typescript-client/#connection-errors","title":"Connection Errors","text":"<pre><code>try {\n    const client = new WebSocketManagerClient({ url: \"http://invalid-host:5001\" });\n    await client.connect({ url: \"wss://invalid\" });\n} catch (error) {\n    console.log(`Connection failed: ${error.message}`);\n}\n</code></pre>"},{"location":"typescript-client/#complete-example","title":"Complete Example","text":"<pre><code>import { WebSocketManagerClient } from \"@virtufin/websocketmanager\";\n\nasync function main() {\n    console.log(\"WebSocketManager Client Demo\");\n    console.log(\"==============================\\n\");\n\n    const client = new WebSocketManagerClient({ url: \"http://localhost:5001\" });\n\n    // Connect\n    console.log(\"Connecting to WebSocket server...\");\n    const { id: connectionId, status } = await client.connect({\n        url: \"wss://echo.websocket.org\",\n        autoReconnect: false\n    });\n    console.log(`Connected: ${connectionId} - ${status}`);\n\n    // List connections\n    console.log(\"\\nAll connections:\");\n    const { connections } = await client.list({});\n    for (const conn of connections) {\n        console.log(`  ${conn.id}: ${conn.url} [${conn.status}]`);\n        if (conn.topic) {\n            console.log(`    Publishing to: ${conn.topic}`);\n        }\n    }\n\n    // Send a message\n    console.log(\"\\nSending message...\");\n    const { response } = await client.send({\n        id: connectionId,\n        message: JSON.stringify({ type: \"request\", id: \"123\", data: \"hello\" }),\n        timeoutMs: 5000\n    });\n    console.log(`Response: ${response}`);\n\n    // Start pub/sub\n    console.log(\"\\nStarting pub/sub...\");\n    await client.startPublish({\n        id: connectionId,\n        topic: \"ws-events\"\n    });\n    console.log(\"Publishing started\");\n\n    // Stop pub/sub\n    console.log(\"\\nStopping pub/sub...\");\n    await client.stopPublish({ id: connectionId });\n    console.log(\"Publishing stopped\");\n\n    // Disconnect\n    console.log(\"\\nDisconnecting...\");\n    await client.disconnect({ id: connectionId });\n    console.log(\"Disconnected\");\n}\n\nmain();\n</code></pre>"},{"location":"typescript-client/#protobuf-dependencies","title":"Protobuf Dependencies","text":"<p>The client library requires the peer dependency:</p> <pre><code>{\n  \"peerDependencies\": {\n    \"@bufbuild/protobuf\": \"^1.10.0\"\n  }\n}\n</code></pre> <p>Ensure your project installs the peer dependency:</p> <pre><code>npm install @bufbuild/protobuf\n</code></pre> <p>The proto files are pre-compiled into the <code>src/generated</code> directory.</p>"},{"location":"typescript-client/#generated-protos","title":"Generated Protos","text":"<p>The client includes generated protobuf classes from <code>websocketmanager.proto</code>:</p> Type Description <code>ConnectRequest</code> Request to connect to a WebSocket server <code>ConnectResponse</code> Response with connection ID and status <code>ListRequest</code> Request to list all connections <code>ListResponse</code> Response with list of connections <code>DisconnectRequest</code> Request to disconnect <code>DisconnectResponse</code> Empty response <code>StartPublishRequest</code> Request to start pub/sub publishing <code>StartPublishResponse</code> Empty response <code>StopPublishRequest</code> Request to stop publishing <code>StopPublishResponse</code> Empty response <code>SendRequest</code> Request to send a correlated message <code>SendResponse</code> Response with server reply <code>SendRawRequest</code> Request to send without waiting <code>SendRawResponse</code> Empty response <code>WebSocketConnection</code> Connection details ## SDK Reference <ul> <li>C# API Reference</li> <li>Python API Reference</li> <li>TypeScript API Reference</li> </ul>"}]}